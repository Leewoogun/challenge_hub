# 백로그 — challenge 프로젝트

> 흩어진 TODO/미해결/후속 작업을 한 곳에서 추적하는 살아있는 문서. 분류는 우선순위 + 담당자.
>
> **자동 갱신**: pm-lead가 `report-and-document` 스킬로 feature를 마무리할 때마다 summary.md의 미해결 이슈를 본 백로그에 자동 추가. 임의 시점 재정리는 "백로그 정리해줘" 한 마디로 pm-lead에게 요청.

- **마지막 갱신**: 2026-09-05 (**feature `verification-photo-replace` completed** — *인증 사진을 마감 전까지 바꿀 수 있다.* 재제출 **전면 거부 → `last-write-wins`**. **마이그레이션 0 / 새 엔드포인트 0 / 요청·응답 shape 변경 0** — 바뀐 건 같은 요청에 대한 **서버의 행동**뿐이다. 서버 **574/574**(50 skipped=Docker) + throwaway DB 실기동 / 모바일 **Android 112 · iOS 73 failures 0**(pm 이 XML 직접 집계). ✅ **2026-09-05 종료 — 양 레포 커밋**(app `2a650ba` / server `e2f57f5`) **+ T-I1 실기 3/4 확인**(내 교체 왕복 · 🔴 상대 교체 → 내 화면 · 알림 1회만 — 단위 테스트가 원리적으로 못 잡던 두 항목이 정확히 앞의 둘이다). 잔여는 **서버 실파일 삭제 육안 1건뿐이고 비블로커**다. 🔑 **실제 난점은 재제출이 아니라 캐시였다** — 사진 경로가 교체돼도 그대로라 서버·앱 양쪽이 옛 사진을 캐시하고 있었고, 그대로 뒀으면 *"다시 찍어도 화면이 안 바뀌는데 에러도 로그도 없는"* 상태로 배포됐다(테스트는 전부 초록). **두 팀원이 각자 계층에서 독립 발견** → 서버 `no-cache`+ETag / 앱 Coil 캐시 키 `photoUrl+verifiedAt`. 🔴 **부수 발견: `:remote:datasource` iOS 테스트가 한 번도 컴파일된 적 없다**(백틱 이름의 `,()` — Kotlin/Native 금지). 🔴 **T-M4 를 조용히 되돌릴 계약 문장 1건 삭제**. 🔴 **pm 자기 정정 3건** — 해제 기준 자기모순(교착 직전) / **승인을 실측표에 ✅ 완료로 기록** / 남의 관측을 확인 없이 내 질문으로 전달. ⚠️ 팀 운영 사고 1건 — 이전 세션 잔여 팀원에게 통지 오발신(무해 종료, `run-feature` 에 잔여 확인 단계 신설))
- 직전 갱신: 2026-09-01 (**feature `notification-list` 완료** — *알림이 오는데 볼 곳이 없던 상태를 닫았다.* 조회 API 3종 + 신규 `:feature:notification` + 홈 벨 뱃지 점등. 🔴 **`implemented`** — ✅ 2026-09-05 정정: 양 레포는 커밋됐고(app `b5e7233` / server `bc18aea`) 남은 미충족은 **실기 미확인** 하나다(서버 12파일·앱 34경로). 서버 589/540 passed 회귀 0 + throwaway DB 실구동 21항목 / 모바일 Android 162 · iOS 162 failures 0. 커서 페이지네이션 프로젝트 최초 도입 → **ADR-0012 accepted**. 부수: `:core:utils` iOS 개통(rename 1건 → 35건), **앱 `PlaceholderScreen` 소비처 0건**. 이 feature 의 수확은 *"조용히 틀리는 것" 5건 적발* — 포맷터 오용(전 행 "마감") · 서버 타입 강등 · 폴백 KDoc 의 거짓 근거 · 정렬 축 · 숫자 5중 복제)
- **직전 갱신**: 2026-08-26(2차) (**feature `mypage` 완료** — *4탭 완성 + 계정 수명주기 마감.* 프로필·계약서 보관함(히스토리 🟡 해소)·로그아웃 실구현(🟡 해소)·회원탈퇴. 서버 464passed+throwaway 실구동 2회 / 모바일 A346·iOS259. photoDeleted additive(pm 지시 번복)·분모 통일(CLAUDE.md 승격)·실패 표시 3원칙. design 오류 6건 전부 사전 적발. 미해결: 실기 0 / KAKAO_ADMIN_KEY / ⑭)
- 직전 갱신: 2026-08-26 (**feature `loser-ranking` 완료** — *패배의 왕좌 개통, 4탭 placeholder 0 달성.* `GET /rankings/losers` 신규 1건(9필드, 마이그레이션 0) + 랭킹 탭 실화면(Top3 포디움+수치의 명단). 서버 **417/417**+실서버 실측(정렬 4키 실데이터 재현·LEFT JOIN 폴백 계정 심어 검증) / 모바일 **A460·iOS148**(계약 필드별 대조 차이 0, 재정렬하면 깨지는 단언). 🔴 **전역 발견 2건**: ADR-0002 "항상 HTTP 200"이 인증 실패엔 거짓(실제 bare 401 — 문서가 틀렸고 서버·앱이 맞았다, 일괄 감사 백로그) / 카카오 프로필 URL 이 http 평문이라 **아바타 켜면 iOS 만 ATS 차단**(https 정규화 선행 조건 등재, fname 함정 포함). 🔴 **팀 자기 교정 3건** — 기각 사유 과장 금지 / 실측 보고와 스코프 판단 분리 / 사실의 소유자는 한 곳(+작성 중 문서로 계약 닫지 않기). 미해결: 디바이스 실기 / 디자이너 확인 12건(⑫ 1위 패배율 병치 포함))
- 직전 갱신: 2026-08-25(2차) (**feature `challenge-result` 완료** — *이제 챌린지가 스스로 끝난다.* 첫 스케줄러(:batch)가 자정 판정+전적 집계를 단일 트랜잭션으로. 서버 392/392 + 모바일 A186/iOS165, 실데이터 소급 실측(judged=6/expired=2, 알림 0 증가, 멱등). 홈 전적 첫 실데이터化 + V10 연패 컬럼 = 개돼지 랭킹 데이터 준비 완료. 백로그 4건 해소(EXPIRED 스케줄러/집계 트리거/연패 컬럼/FCM 제외 결정 이행). 미해결: 디바이스 실기 / 결과 UI 디자이너 확인 / 히스토리 화면)
- 직전 갱신: 2026-08-25 (**feature `challenge-verification` 완료** — *핵심 플로우의 "양측 인증(카메라)" 구간 개통.* 촬영(갤러리 차단)→리사이즈·EXIF 제거→multipart 제출→`VERIFIED`+`OPPONENT_VERIFIED` 푸시→상대가 상세에서 사진 확인까지 **Android 실기 왕복 검증**. 부수로 챌린지 상세가 Lovable 기준 전면 재구성(VS 헤더·미션 카드 분리·남은 시간)됐고 **앱 첫 원격 이미지 스택(Coil 3)** 도입. 백엔드 **320/320** + 모바일 detail 29/29 JVM·iOS 등 전 모듈 초록 + **T-M2 실측 달성**(실사진 115~138KB ≪ 목표 500KB). 🔴 **개발 중 사용자 결정으로 계약 2회 개정** — 업로드 URL발급→multipart(8/18), 조회 서명URL→JWT 서빙(8/18) — 서명 체계·전용 클라이언트·만료 갱신이 통째로 사라져 최종 구조가 단순해졌다. 🔴 실기에서 "테스트 초록인데 깨진" 2건 — 로거의 사진 스트림 소비(무해한 결정 3개의 교차점), storage-root 변경으로 옛 사진 404(정상 404와 구분 불가). 미해결: 문구 4종 초안 / 디자이너 확인 8건 / iOS 촬영 실기기 / 판정 feature(FCM 제외 확정))
- 직전 갱신: 2026-08-08 (**feature `push-deeplink` implemented** — *알림이 목적지를 갖게 됐다.* `push-fcm` 미해결 🟢 "딥링크 미구현"을 닫았고, 그 문서의 예언대로 **서버 변경은 `tag` 추가 한 건**뿐이었다(`notification`·`data` 무접촉). **서버 260/260**(254→260, 회귀 0) + **모바일 267/267**. 🔴 **설계 문서를 그대로 구현하지 않았다** — 백엔드 요청 5건 중 3건은 이미 끝나 있었고(*"가장 시급"*이라던 `challengeId` 키 확인은 실기기 없이 해소), 반대로 **실기 발화 최다인 `CHALLENGE_REQUEST`가 통째로 빠져 있었다.** 그대로 짰으면 가장 흔한 알림에서 조용히 실패했다. 🔴 **pm-lead가 근거를 틀려 정정한 건 1건** — *"REJECTED는 Detail이 Error로 깨진다"*고 적었으나 서버는 상태를 아예 안 본다. **화면 이름만 보고 추측한 것을 확인한 것처럼 적었다**(#25 계열). 부수로 **`push-fcm` 계약 §3의 세 번째 드리프트**를 잡았다 — *"3종 전부 확정"*의 근거로 인용한 커밋이 오히려 2종을 초안으로 표시한 커밋이었다. 🔴 **미해결: 실기 검증 0건 + 미배포 + 양 레포 미커밋**)
- 직전 갱신: 2026-08-07 (**feature `push-fcm` 완료** — *앱이 사용자에게 먼저 말을 걸 수 있게 됐다.* 세 feature에서 반복 지적되던 *"홈에 들어와야 도전장을 안다"* 결함 해소. **백엔드 254/254**(baseline 192 → 신규 62, 회귀 0) + **모바일 빌드 초록**. 🔴 **실기 검증 완료** — 알림 3종 전부 발화, 그리고 **§0.2 토큰 소유권 이전이 실사용으로 증명됐다**(같은 기기 계정 전환 시 토큰이 15→16으로 이동하고 15는 NULL). 그 SQL은 fake만 태우던 무검증 상태였다. **`NoOpNotificationSender` 격리 덕에 Firebase 프로젝트를 기다리지 않고 백엔드 전체를 완성**했고, 키 없이 돌린 실행이 오히려 격리 동작을 실기에서 증명했다. `logout` 미구현(foundation 시절 TODO) 흡수. ⛔ **iOS 수신은 범위 제외 — 기술 선택이 아니라 APNs 키가 유료 Apple 계정 전용이라 경로가 존재하지 않는다.** ADR-0005가 처음 깨지는 지점)
- 직전 갱신: 2026-08-06 (**기획서 전면 개정** — 노션 [🔥 챌린지 앱 기획서](https://app.notion.com/p/3443902cbe2480ab8f9dc21aea474de8)를 구현 상태 기준으로 재정의. **삭제 2건**: 전화번호부 연동·전화번호 검색(§2.2→§6), AI STT(§2.4). **축 반전 1건**: 명예의 전당 → **개돼지 랭킹**(승자 랭킹 → 패자 랭킹) — Lovable `ranking.tsx`에만 반영돼 있고 기획서·PM 허브에 전파된 적 없던 변경을 정식 반영. **추가**: 챌린지 수락/거절 알림, 신청 취소·자동 만료, 판정 시점, 재대결, 4탭 진입 경로. ADR-0006 **superseded** / ADR-0008 **deferred**. 개정 전 원문은 [notion-planning-snapshot-2026-08-06.md](./product/notion-planning-snapshot-2026-08-06.md)에 박제. 🔴 **백로그 자체의 stale 2건도 실측 정정** — 아래 참조)
- 직전 갱신: 2026-08-06 (#25 전 계약 감사 — 백엔드 **192/192** passed, `WireShapeContractTest` 13건 신설. 🔴 nullable 축이 아니라 **시간 포맷 축**에서 능동적 오류 발견: `friends` 계약이 서버가 보내지 않는 `Instant`/ISO-8601을 보낸다고 적고 있었다. 원인은 도구가 아니라 **ADR-0010의 커버리지 누락**. mobile-dev가 `RefreshResponse`가 `ApiResult` 경로 밖임을 발견 → 가드 구현했다가 **사용자 지시로 전량 되돌림**. 🔴 **가정에 방어 로직을 넣지 않는다**가 원칙으로 섰다 — 아래 🟢 항목 참조)
- 직전 갱신: 2026-08-03 (`soul-oath` partially-completed — 양측 서명 없이는 챌린지가 시작되지 않는다. 백엔드 171/171 + 실서버 56/56, 모바일 230/230, Android 실기 통과. 🔴 **실기에서 버그 3건** — 전부 테스트가 초록인 채였다. 미해결: iOS 실기 / 주관 3건 / 통합 45 skip / 3개 레포 커밋 0건)
- **우선순위 기호**: 🔴 긴급(블로커) / 🟡 중요 / 🟢 일반 / 🔵 대기(외부 의존)
- **담당 약어**: pm / mobile / backend / design / user(사용자가 직접 처리)

---

## 🔴 긴급 (블로커)

| 항목 | 출처 | 담당 | 메모 |
|---|---|---|---|
| 🔴 **`:remote:datasource` iOS 테스트가 한 번도 컴파일된 적 없다** | [verification-photo-replace/summary.md](./features/verification-photo-replace/summary.md) | mobile | Kotlin/Native 가 백틱 테스트 이름의 `,` `(` `)` 를 금지하는데 그 모듈이 위반한다(**15건**, `ChallengeRemoteDataSourceImplTest`·`LoginRemoteDataSourceImplTest`). 🔴 **증거: `build/test-results/` 에 iOS 디렉터리 자체가 없다**(`testDebugUnitTest`·`testReleaseUnitTest` 뿐). 즉 **이 모듈의 iOS 커버리지가 0** 이고 그동안 Android 에서만 검증돼 왔다. 없던 타겟을 불러서 드러났다 — 안 불렀으면 계속 안 보였을 결함이다. 다른 모듈들은 이미 제약을 지키고 있어 **이 모듈만 예외**로 보인다. 고치려면 테스트 이름 15개+ 개명이 필요하다. |
| 🔴 **`push-deeplink` 실기 검증 0건 + 미배포** | [push-deeplink/summary.md](./features/push-deeplink/summary.md) | user + mobile | **T-I1 5케이스(포그라운드 / 백그라운드 앱 생존 / 앱 종료 / 로그아웃 상태 탭 / 화면 회전)를 하나도 실행하지 않았다.** 기기·에뮬레이터·기동 중인 백엔드가 없었다. ⚠️ **spec이 "이번 설계의 핵심"으로 지목한 로그아웃 상태 탭 / 화면 회전이 미검증분에 들어간다** — 단위 테스트가 게이트 로직과 `KEYS` 완전성은 고정하지만 **`intent.removeExtra`가 실제 Activity 재생성에서 먹는지는 단위 테스트 밖**이다. 🔴 **tag 중복 제거는 자동 테스트로 증명 불가하다** — 증명된 건 *"올바른 tag를 실었다"*까지고 *"트레이에 하나만 남는다"*는 Android 시스템의 몫이라 **서버 배포 후 실기가 유일한 경로**다. `soul-oath`에서 *"테스트가 초록인 채 실기 버그 3건"*이 나온 선례가 있다. |

## 🟡 중요

| 항목 | 출처 | 담당 | 메모 |
|---|---|---|---|
| 🟡 **`MissionCardMineFailedPreview` 가 실제로 안 나오는 화면을 그린다** | [verification-photo-replace/summary.md](./features/verification-photo-replace/summary.md) | mobile | `FAILED` → 슬롯 `NONE` 인데 Preview 는 *"다시 인증하기"* 버튼을 그린다. 🔵 **기획 판단은 이미 나와 있다 — `FAILED` 에 재시도를 열지 않는다.** `ai-verification` 이 자정 배치 판정으로 확정돼 있어 `FAILED` 가 찍히는 시점엔 이미 마감이고 **재시도 창 자체가 존재하지 않는다.** 따라서 **현재 코드가 옳고 Preview 가 낡았다** — 삭제하거나 실제 상태에 맞게 고친다. challenge-verification 때부터 어긋나 있었다. ⚠️ **`FAILED` 가 실제로 발생하기 시작하는 건 `ai-verification` 부터**이므로 그 feature 에서 함께 처리하는 것이 자연스럽다. |
| 🟡 **모바일 코드 정리 — `flowUtil` 신설** | 사용자 지시 (2026-08-03) | mobile | **다음 모바일 작업의 우선 항목.** ⚠️ **2026-08-06 위치 실측 정정** — 원래 문구였던 *"6개 RepositoryImpl에 10곳"*은 더 이상 맞지 않다. 사용자 커밋 `0389e99 refactor: 프로젝트 아키텍처에 DataSource 추가`로 `flow { }`가 **`:remote:datasource`로 이관**됐고 RepositoryImpl은 얇은 위임체가 됐다. **사본은 그대로 9곳**(`Login` 2 / `Friends` 3 / `Challenge`·`Record`·`ActiveChallenge`·`UserInfo` 각 1). 패턴은 `flow { api().suspendOnSuccess { emit(map()) }.suspendOnFailureWithErrorHandling(onError) }` 가 매번 손으로 복제되는 구조라, 한 곳이 바뀌면 나머지가 조용히 갈린다 — `soul-oath`에서 `UnknownApiError`가 `onError`를 안 부르던 결함이 **전 repository에 동시 영향**이었던 것과 같은 결이다. ⚠️ **2026-08-14 근거 정정 — 원래 근거였던 *"인증 feature 착수 전에 접어라(`VerificationRemoteDataSource`가 10번째 사본을 만들기 전에)"* 는 무효다.** 인증 feature 는 **사진 저장 방식 미결정으로 착수 자체가 불가**하다(업로드 인프라 grep 0건). 시간 압박이 사라졌다. **그래도 지금이 적기인 이유는 따로 있다**: (1) 모바일 working tree 가 **clean** 이라 전 datasource 를 훑는 리팩터가 다른 미커밋 변경과 겹치지 않는다 — 겹치면 리뷰가 불가능해진다, (2) **서버 선행이 필요 없는 유일한 모바일 작업**이다(알림목록·랭킹·인증 전부 서버/디자인 대기), (3) 회귀 안전망이 있다(`remote:datasource` 22건 + `data:repositoryImpl` 14건 초록). 🔵 **"10번째 사본 방지"는 알림 목록 feature 로 성립한다** — `NotificationRemoteDataSource` 가 정리된 헬퍼의 첫 소비자가 되게 순서를 잡으면 된다. (`:core:utils`의 기존 `FlowUtil.kt`는 **다른 것**이다 — StateFlow `withData`/`updateWithData` ViewModel 헬퍼라 이 건과 무관.) |
| 🟡 **알림 문구 2종 미확정 + 전역 톤 통일** | [push-deeplink/summary.md](./features/push-deeplink/summary.md) · [push-fcm/summary.md](./features/push-fcm/summary.md) | user | **사용자 결정 사안.** `CHALLENGE_REQUEST`만 ✅ 확정(격식체)이고 `CHALLENGE_ACCEPTED`(`계약 완료.`) / `CHALLENGE_REJECTED`(`ㅠㅠ`)는 🟡 **초안값**이다. 🔴 **2026-08-08 계약 §3이 이걸 "3종 전부 확정"으로 잘못 요약한 것을 정정했다** — 근거로 인용한 커밋 `e76c64c`가 오히려 2종을 초안으로 표시한 커밋이었고, **문자열 교체와 문구 확정을 같은 사건으로 읽은** 것이다(세 번째 드리프트). 부수로 KDoc 오기도 잡혀 **갈린 축이 존댓말/반말이 아니라 `title`**임이 밝혀졌다(`body`는 셋 다 합니다체). ⚠️ **임의로 통일하지 마라 — 확정분을 초안에 맞추는 방향은 특히 안 된다**(`NotificationMessages` KDoc 지시, 유효). 문구는 `NotificationMessages` 한 곳에만 있어 확정 시 그 파일만 고치면 된다. |
| 🟡 **모바일 하네스 갭 — 신규 core 모듈 생성이 매번 수작업** | [challenge-verification/mobile-report.md](./features/challenge-verification/mobile-report.md) | mobile | `:core:push`(push-fcm) → `:core:permission` → `:core:camera`(challenge-verification)로 **세 번째 반복**이다. ⚠️ `challenge-app` 의 Skill 매핑 테이블에 **신규 core 모듈 생성 스킬이 없다.** 🔴 게다가 기존 `scripts/create-core-module.sh` 는 **쓸 수 없는 상태다** — pm-lead 실측: `PACKAGE_PATH="com/lwg/base/$MODULE_NAME"` 인데 프로젝트 패키지는 `com.lwg.challenge` 다(ADR-0003 리네임 이후 스크립트가 안 따라갔다). `namespace = "com.lwg.MODULE_NAME"` 도 치환이 어긋나 있다. **mobile-dev 가 스크립트를 못 쓰고 손으로 만들었다.** 조치는 둘 중 하나 — 스크립트를 고치거나, 스킬로 승격하거나. **네 번째 모듈이 생기기 전에 정하는 게 이득**이지만, 착수를 막는 항목은 아니다. |
| 🟡 **`.setToken` deprecation (firebase-admin 9.10.0)** | [push-deeplink/summary.md](./features/push-deeplink/summary.md) | backend | **기존 코드이고 `push-deeplink`와 무관하다** — 도입 커밋 `f01bd44`(push-fcm), 이번 diff의 삭제 라인 0, firebase-admin 버전도 그 커밋 이래 무변경. 재컴파일로 경고가 다시 표면화됐을 뿐. ⚠️ **한 줄 교체가 아니다** — `setFid`가 deprecated가 아닌 건 확인됐으나 **등록 토큰의 대체재라는 근거가 없다**(FID는 다른 식별자). 바꾸면 `users.fcm_token` 컬럼 · `confirmed` 계약 §1(`PUT /users/me/fcm-token`) · 모바일 `FcmTokenProvider`·`onNewToken`을 함께 건드리는 **교차 레포 작업**이 된다. **컴파일 경고 하나 지우려고 `confirmed` 계약과 스키마를 건드리는 건 비용 방향이 반대다.** 착수 전 선결: *"`setFid`가 등록 토큰의 대체재인지, 별개 타겟팅 수단인지"* Firebase 문서로 확정. |
| ~~🟡 `user_stats` 연패 컬럼 부재 → 개돼지 랭킹 데이터 미충족~~ ✅ **2026-08-25 해소** — challenge-result V10 으로 `current_loss_streak`/`max_loss_streak` 추가 + 판정 집계에서 갱신(연승의 거울상 규칙). 실DB 적용·실데이터 대조 완료. 예약대로 "판정 feature 스코프"에서 이행됨 | 기획서 §3.4 개정 (2026-08-06) · Lovable `ranking.tsx` | backend | `user_stats`는 `wins/losses/draws/current_streak/max_streak`을 갖는다. 랭킹이 쓰는 `losses`(패수)와 `lossRate`(= losses/total_challenges)는 바로 나오지만 **`lossStreak`(연패)에 대응하는 컬럼이 없다.** ⚠️ `current_streak`/`max_streak`의 부호를 뒤집어 재활용하면 **홈 StatsBar가 음수를 받는다**(home-feed에서 "연승"으로 이미 표시 중). → `current_loss_streak` / `max_loss_streak` 2개를 V8로 추가. `user_stats`는 **자동 집계 트리거 자체가 미구현**이라 결과 판정 feature에서 집계 로직을 처음 쓰게 되므로, **그때 같이 넣으면 추가 비용이 사실상 0**이다. 판정 feature 스코프. |
| 🔴 **`soul-oath` iOS 실기 미검증** (서명 캔버스 `pointerInput`) | [soul-oath/summary.md](./features/soul-oath/summary.md) | user + mobile | **기기 없음.** Compose `pointerInput` 드로잉은 KMP **선례 0건**이고 **지연·좌표 정확도는 손가락이 아니면 안 나온다.** ⚠️ **시뮬레이터로 대체하지 않았다** — 마우스 드래그는 손가락과 다르다. Android 실기에서는 통과(획 픽셀 97→13,701 / 인셋 박스 밖 0px / 종횡비 2.0000). |
| **`soul-oath` 주관 판단 3건** (지연 체감 / 획이 각져 보이는가 / 캔버스 170dp 충분한가) | [soul-oath/summary.md](./features/soul-oath/summary.md) | user + design | 앱에서 **서명 한 번 그어보면** 된다. 각져 보이면 솎아내기 임계(정규화 거리 0.003)를 낮춘다. design.md §7 실기 항목이 여기 녹아 있다. |
| **자정 경계에 위저드 힌트와 계약서 날짜가 갈릴 수 있다** | [soul-oath/summary.md](./features/soul-oath/summary.md) | backend + mobile | 클라 시계로 계산한 힌트(`7/28 24:00`)와 서버가 **요청 도착 시점**에 정하는 `challengeDate`가 초 단위 창에서 어긋나고, `soul-oath`가 그 결과를 **계약서에 영구 기록**한다. 🔴 **"클라 시계를 맞추면 된다"가 아니다** — 시계가 완벽해도 서명→도착 사이에 직렬화·네트워크가 들어간다. 진짜 고치려면 클라가 의도한 날짜를 보내고 서버가 존중해야 하는데 그건 `deadlineType` enum의 취지(**기기 시계 조작·타임존 불일치 차단**)와 맞바꾸는 것 — **버그 수정이 아니라 트레이드오프 재검토**다. 현재 판단은 안 고치는 쪽. |
| 🔴 **컨테이너 런타임 부재 → 백엔드 통합 테스트 45건 누적 미실행** (기존 24건 + challenge-create 21건) | [challenge-create/backend-report.md](./features/challenge-create/backend-report.md) · [auth-kakao](./features/auth-kakao/backend-report.md) · [home-feed](./features/home-feed/change-log.md) | user + backend | **2026-07-28 pm-lead 실측: docker/podman/colima/orbstack/nerdctl 전부 미설치, Docker.app·OrbStack.app 번들도 없음.** 지금까지 이 프로젝트는 통합 테스트를 **단 한 번도 실행한 적이 없다** — 매 feature마다 자동 skip되어 리스크가 누적 중. challenge-create는 V5 마이그레이션 + 부분 유니크 인덱스 + native 쿼리가 DB를 타야만 검증되는 구조라 특히 아프다. backend-dev가 로컬 Postgres 16.13에 일회용 DB로 SQL 레벨 대체 검증(마이그레이션 4건 / 인덱스 8케이스 / native 쿼리)은 마쳤으나 **JPA 매핑·Flyway 자동 적용·Security 필터·직렬화가 맞물리는 층은 미검증.** → 런타임 하나(Docker Desktop / OrbStack / Colima 아무거나) 설치 후 `./gradlew :app:test` 일괄 실행 필요. **배포 전 필수 관문.** 🔴 **2026-08-28 추가 — 설치 시 1순위 자동화 대상**: 보관함 월 조회의 **말일 챌린지 경계**(7/31 건이 7월 응답에 있고 8월에 없다). 단위 테스트는 fake 를 태워 *"구현과 fake 가 함께 틀리면 통과"* 하고 실제 SQL 축은 손으로만 확인됐다. **앱측 월 버킷팅이 제거돼(서버 정렬·필터 신뢰) 어긋나면 카드가 조용히 사라지는데 앱에는 검증 수단이 0이다.** |
| 🔵 **`dev-test-login` 격리(꺼진 상태) 실서버 검증 — 배포 전 관문** | [dev-test-login/summary.md](./features/dev-test-login/summary.md) | user + backend | **개발 중에는 켜둔 채로 두는 것이 정상이다** (2026-08-03 사용자 결정 — 개발 끝날 때까지 유지). 이 검증이 막으려는 위험("테스트 로그인이 운영에 새어나감")은 **배포 시점에만 현실화**되므로 그때 확인한다. 현재 단위 6케이스(실제 Spring 컨텍스트에 태워 빈 미생성 확인)는 통과 상태. **배포 전 절차**: VM options에서 `-Dchallenge.dev.test-login.enabled=true`를 빼고 기동 → backend-dev가 `e2e-off.sh`(9단언) 실행 → 확인 후 운영 배포. ⚠️ `/actuator/health` 404가 **test-login 경로의 404를 자동 보장하지 않는다** — 같은 핸들러여도 permitAll 상시 적용이 함께 걸려야 성립한다. |
| **챌린지 수락 경로 DB 증거 부재** | [dev-test-login/summary.md](./features/dev-test-login/summary.md) | user | 2026-08-03 사용자 손 검증 완료 확인. 단 직후 `challenges=0`이라 **수락 경로(`IN_PROGRESS` 전환 + `verifications` 2건 생성)의 DB 레벨 증거가 없다.** 취소가 물리 삭제이므로 "신청→취소"만 돌았다면 정상. 증거가 필요하면 신청→수락으로 한 번 더. |
| 영혼의 맹세(STT + 서명) 화면 디자인 | 노션 [📋 Common States & PM Questions](https://app.notion.com/p/3523902cbe248111ac2dd40fcd8fda64) | design + user | 핵심 플로우인데 Lovable에 화면 부재. 디자이너 작업 필요. |
| ~~전화번호 등록 화면 UX (SMS 인증 제거 후)~~ | ~~노션 동상 / ADR-0008~~ | ~~design + user~~ | ✅ **2026-08-06 폐기** — 기획서 §2.2에서 전화번호부 연동·전화번호 검색이 §6 향후 확장으로 이동. 등록 화면 자체가 IA에서 삭제됐다. **디자이너 대기 화면이 2개 → 1개(영혼의 맹세)로 줄었다.** 되살릴 때의 선결 조건은 아래 🟢 "전화번호 해시 pepper" 참조. |
| 디자이너 시각 검증 6건 (gradient end / card stops / chart4 / glow brush / 135deg / SoulStampLogo 회전 제거 후 시각 균형) | [colors.md §5](./design-system/colors.md) | design + user | oklch→hex 자체 변환분 + 외곽 회전 링 제거 결정 후 시각 균형. 디자이너 부재로 추정값 그대로 채택 중. |

## 🟢 일반

| 항목 | 출처 | 담당 | 메모 |
|---|---|---|---|
| 🟢 **`verification-photo-replace` T-I1 ④ 서버 실파일 삭제 육안 미확인** | [verification-photo-replace/summary.md](./features/verification-photo-replace/summary.md) | user | ✅ **2026-09-05 T-I1 3/4 통과로 강등된 잔여분.** 위험했던 ①②③(내 교체 왕복 · 🔴 상대 교체 → 내 화면 · 알림 1회만)은 사용자 육안으로 닫혔다 — **캐시가 이 feature 의 실제 난점이었고 단위 테스트가 원리적으로 못 잡던 항목이 정확히 그것들이다.** 남은 건 *"교체 후 서버 폴더에 이전 파일이 안 남는가"* 하나. 🔵 **비블로커인 이유 둘**: ⓐ `FakePhotoStorage` 로 호출·삭제 key·**커밋 후 순서까지** 단언돼 있다(테스트 4건) ⓑ `PhotoStorage.delete` 는 이 feature 가 만든 코드가 아니라 **회원탈퇴가 이미 쓰던 기존 구현**이다. 실패해도 증상은 **고아 파일 잔존뿐이고 교체 자체는 성공**한다(spec §6 · ADR-0011 보관 기간 정책 소관). |
| ~~🔵 판정(결과) feature 스코프 결정 — RESULT·REMIND FCM 알림 제외~~ ✅ **2026-08-25 이행 완료** — challenge-result 가 이 결정대로 구현(notifications 14→14 실측, 미발송을 퍼블리셔 미주입으로 구조 보장). **추후 푸시 도입 시**: 배관(NotificationDispatcher) 재사용 + 타입·문구·딥링크 추가 + §0.6.1 모바일 통지 선행 | challenge-verification 후속 계획 논의 → [challenge-result/summary.md](./features/challenge-result/summary.md) | pm + backend | 다음 feature 인 판정 배치는 **DB 전이만** 한다 — 자정 직후 `deadline` 경과 `IN_PROGRESS` 를 §2.6 규칙(양측 인증=무승부/한쪽만=승/양측 미인증=양패)으로 `COMPLETED`+`result` 전이, 미인증 `verifications` 는 `FAILED`. **`RESULT`(결과 통지)·`REMIND`(마감 1시간 전) 푸시는 만들지 않는다.** ⚠️ 인지된 트레이드오프: 사용자는 앱에 들어와야 결과를 안다 — push-fcm 이 해소했던 *"홈에 들어와야 안다"* 패턴의 재수용이며, 나중에 붙일 때 배관은 그대로라(NotificationDispatcher 재사용) 타입·문구·딥링크 추가만으로 열린다(§0.6.1 모바일 통지 선행 규약 준수). 판정 feature 스펙 작성 시(feature-spec) 이 행을 근거로 범위에서 제외할 것. |
| **탈퇴 후 access token 최대 1시간 잔존** (수용된 트레이드오프) | 2026-08-26 mypage 계약 ([spec](./features/mypage/spec.md)) | backend | 요청마다 계정 상태를 확인하려면 **전 인증 요청에 DB 조회 1회**가 붙어 전역 회귀 위험 — 이번 범위 제외. 앱이 탈퇴 즉시 로컬 토큰을 지워 실사용 경로는 차단(계약 수용 기준). 실사용자 유입·보안 요구 상승 시 재검토(EXIF 서버측 부채와 같은 만기). |
| ~~🟢 페이지네이션 일괄 도입 **방식** 결정~~ → 🟢 **페이지네이션 적용 확대 "시점"** (방식은 확정) | 2026-08-26 mypage 실측(목록 엔드포인트 **5종**이라 적었으나 ADR-0012 실측은 **6종** — 아래 정정) → 2026-08-28 개정 → 2026-09-01 방식 확정 | pm + backend + mobile | ✅ **2026-09-01 방식 = 커서 확정 → [ADR-0012](./decisions/0012-list-pagination-cursor.md) `accepted`** (`?cursor={lastId}&size=N` → `nextCursor: Long?`, `id DESC`). CLAUDE.md 규칙 + ADR 양쪽에 등재 — 규칙 줄은 결론을, ADR 은 그 줄이 잃게 될 근거를 보관한다. ⚠️ **여기 적힌 "5종"은 부정확했다** — ADR 실측은 **6종**이다(`/friends`·`/friends/requests/received`·`/rankings/losers`·`/challenges/active`·`/challenges/received`·`/challenges/history`). notification-list 가 최초 도입 — 알림은 머리에 append 되는 목록이라 offset 이 **상시** 깨진다. **남은 것은 "언제 나머지 5종에 적용하나"**(알림 제외한 6종 중 보관함은 월이 이미 경계) 뿐이고 shape 은 확정된 것을 그대로 쓴다. 🔴 2026-08-28 철회된 근거: ~~"보관함이 프로젝트 최초의 무한히 자라는 목록"~~ — 월 단위 조회 개정으로 한 응답 상한이 한 달치로 고정돼 전제가 소멸(1순위 트리거였다). 남은 만기 신호는 친구·랭킹이 수백 건 규모에 도달하는 경우. ⚠️ 보관함은 월이 이미 경계라 따라 고치지 않는다. |
| **`MethodArgumentTypeMismatchException` 전역 핸들러 부재 → 타입 불일치가 HTTP 500 + code=500** | 2026-08-28 보관함 월 개정 중 실측 ([mypage/api-contract §5](./features/mypage/api-contract.md)) | backend | `handleUncaught` 로 떨어져 **ADR-0002 의 "5xx 는 인프라 장애 전용" 과 어긋난다.** 보관함은 `month` 를 `String` 으로 받아 우회했으나 **타입 있는 `@RequestParam`/`@PathVariable` 을 쓰는 엔드포인트가 생기면 같은 구멍이 열린다.** 전역 핸들러 추가는 기존 모든 응답의 에러 표면을 바꾸므로 별건. |
| ~~🟡 결과 히스토리(지난 챌린지) 화면 부재~~ ✅ **2026-08-26 해소** — mypage 의 **계약서 보관함**(`GET /challenges/history` + 월별 카드 목록)이 정식 해법. 홈 7일 노출은 "최근" 편의로 존속 | 2026-08-25 challenge-result 계약 협의 중 result-mobile 실측 ([spec.md 리스크](./features/challenge-result/spec.md)) | design + mobile + backend | 홈 active 의 COMPLETED 한시 노출(판정 후 **7일**)이 닫히면 **그 챌린지는 앱 어디에서도 도달 불가** — 히스토리 화면이 없고(mypage 는 전적 숫자만) 푸시도 없다(FCM 제외 결정). 판정 결과가 제품의 종착점인데 열람 기한이 있는 셈. 7일은 시간벌기. 지난 챌린지 목록 화면(+조회 API) 이 정식 해법 — 개돼지 랭킹·마이페이지 계열 feature 때 함께 볼 후보. |
| 🟡 **"항상 HTTP 200" 문구 일괄 감사 — 인증 실패는 bare HTTP 401 이다** | 2026-08-26 loser-ranking 실측 (rank-backend, `GET /record` 바이트 대조) | pm + backend | `UnauthorizedEntryPoint` 가 필터 단계에서 HTTP 401 을 직접 쓴다 — ADR-0002 표의 "code=401" 은 실현된 적 없는 문서 오류(ADR 에 정정 각주 완료, repos.json 정정 완료). **각 feature 계약이 같은 문장을 복사해 갖고 있을 가능성** — #25 감사 선례대로 `docs/features/*/api-contract.md` 전수 grep 으로 "항상 HTTP 200"·"code=401" 서술 정정 필요. 앱 동작(Ktor Auth 가 HTTP 401 반응)이 정본이다. |
| **`UserEntity` 에 Spring Data 리포지토리 2개** (기존 + `LoserRankingJpaRepository`) | [loser-ranking/backend-report.md](./features/loser-ranking/backend-report.md) | backend | 버그 아님 — 지금은 둘 다 명시적 주입이라 무해. `Repositories.getRepositoryFor(UserEntity)` 같은 타입 기반 조회가 들어오면 모호해지는 **지뢰**로 기록. |
| **T-M4 잔여 검증 3건** — Coil 404 무캐시 런타임 확인 / 남은 시간 티커 갱신 검증(VM 시계 주입 필요) / CTA 첫 화면 이탈(소형 기기+큰 폰트) 실기 확인 | [challenge-verification/mobile-report.md](./features/challenge-verification/mobile-report.md) | mobile | 404 무캐시는 Coil 3 소스 대조까지만(NetworkFetcher 가 비-2xx 를 캐시 기록 전에 throw). 티커는 "조작된 통과보다 정직한 미검증" 선택 — 검증하려면 VM 에 시계 주입(DI 변경). |
| **iOS 촬영 실동작 실기기 검증** | [challenge-verification/summary.md](./features/challenge-verification/summary.md) | mobile + user | 시뮬레이터에 카메라가 없어 `NO_CAMERA` 분기로 빠진다. 컴파일·단위 테스트는 통과. soul-oath iOS 실기 미검증과 같은 축 — 실기기 확보 시 일괄. |
| **인증 사진 잔여 4건** — 보관 기간 정책(현재 단조 증가) / `photo_url` 컬럼 개명(값은 key 인데 이름이 url, V9 주석이 근거 보존) / multipart 5MB 상한 테스트 미고정 / 제출 실패 시 고아 파일 | [challenge-verification/backend-report.md](./features/challenge-verification/backend-report.md) | backend | 넷 다 ADR-0011 이 인지하고 받은 부채 계열. `PhotoStorage.delete()` 는 ✅ 2026-08-26 mypage 회원탈퇴가 첫 호출부가 됐다(실동작 검증 완료). 보관 정책은 여전히 미결. |
| 🟡 **`storage-root` 상대 경로 — 배포 시 절대 경로 필수** | 2026-08-25 실기에서 실증 ([challenge-verification/summary.md](./features/challenge-verification/summary.md)) | backend | `../challenge-image` 가 서버 실행 디렉터리 기준이라 실행 위치가 바뀌면 루트가 움직인다. **루트 변경으로 이전 사진 전부가 "정상 404 와 구분 불가능한 404" 가 되는 것을 실기에서 실제로 겪었다.** 배포 시 `CHALLENGE_PHOTO_ROOT` 절대 경로로 고정 — ADR-0007 환경 분리에서 다룬다. |
| **프로세스 사망 후 딥링크 중복 소비** (의도적 미방지 — 조치 없음) | [push-deeplink/summary.md](./features/push-deeplink/summary.md) | mobile | `intent.removeExtra`는 **메모리상 `Intent` 객체만** 고친다. 프로세스가 죽고 시스템이 저장한 원본 intent가 복원되면 extras가 살아 있어 **같은 알림으로 한 번 더 이동**한다. ⚠️ **버그가 아니라 판단이다** — *"알림으로 들어왔다가 앱이 죽어 재실행되는"* 상황이라 다시 이동하는 편이 오히려 자연스럽다. 설계 문서 판단을 pm-lead가 승인했다. 필요해지면 `SavedStateHandle`에 소비 여부를 남긴다. |
| **`challengeId?.toInt()` Long→Int 절단** (기록용 — 조치 없음) | [push-deeplink/summary.md](./features/push-deeplink/summary.md) | mobile | `buildNotificationId`가 `Long`을 `Int`로 자른다. `challenges.id`가 `Int.MAX_VALUE`(21억)를 넘으면 서로 다른 챌린지가 같은 알림 id를 갖는다. **관측된 적 없고 현실적이지 않다**(테스트 데이터가 두 자리). 알림 id 체계를 다시 볼 일이 생기면 같이 본다. 🔴 **"싸니까 미리 고치자"는 근거가 아니다** — 아래 `refreshTokens` 항목의 원칙과 같은 축. |
| **`refreshTokens` 경로는 `ApiResult` 사각지대다** (사실 등재 — 조치 없음) | #25 감사 / #26 되돌림 | mobile | **관측된 구조적 사실만 적는다.** `KtorfitModule`의 `refreshTokens { }`는 Ktor `Auth(bearer)` 플러그인 안에서 **원시 Ktor 호출**로 `RefreshResponse`를 받는다(`response.body<RefreshResponse>()`). Ktorfit 인터페이스를 타는 응답 타입 17개와 달리 **`ApiResultConverterFactory` / `suspendOnFailureWithErrorHandling`를 전부 우회**하므로, 앱의 표준 에러 처리(`UnknownApiError → onError` 포함)와 회귀 테스트가 이 경로엔 닿지 않는다. ⚠️ **"그러니 가드를 넣자"는 결론을 붙이지 않는다** — 2026-08-06 사용자 지시로 blank 토큰 가드를 **넣었다가 전량 되돌렸다.** 근거: 해당 시나리오는 **발생 0건 + 재현 방법 없음 + 서버가 앞으로 필드명을 바꿔야 성립**하는 가정이었다. **가정에 대한 방어 로직은 (1) 읽는 사람이 "이런 일이 있었나 보다"로 오해하고 (2) 붙은 테스트가 검증된 동작처럼 보이지만 실은 가정만 검증하며 (3) 구조 변경까지 딸려온다.** 실제 문제가 **관측되면** 그때 이 항목을 근거로 쓴다. 🔴 **`ApiResult` 경로로 편입하는 리팩터는 금지** — refresh는 플러그인 내부라 재귀가 생긴다. |
| **보낸 도전장 목록 조회 + 취소 UI** (`GET /api/v1/challenges/sent` + 홈 "보낸 도전장" 섹션 + 취소 버튼) | [challenge-create/spec.md 스코프 결정 6](./features/challenge-create/spec.md) | backend + mobile + design | **challenge-create에서 옵션 C로 강등 (2026-07-28 사용자 결정).** 서버 `DELETE /api/v1/challenges/{id}`는 이미 구현·테스트 완료 상태라 목록 조회만 붙이면 살아난다. 현재는 생성 직후(create 응답의 `challengeId` 보유 시)에만 도달 가능하고 앱 재시작 후 취소 불가. 서버 비용은 `/received` 대칭 쿼리라 거의 0, 실제 비용은 design 섹션 1건 + 모바일 T-M5 대비 약 +25%(홈 `combine` 소스 +1). ⚠️ `/pending` + `direction` 필드 통합안(B)은 **채택 금지** — 방향별 표시 필드가 달라 한 DTO에 합치면 모바일 기본값 방어 패턴 탓에 오독이 컴파일·런타임 모두 조용히 통과한다. |
| 🔴 **`AFTER_COMMIT` / `REQUIRES_NEW` 실동작이 검증되지 않았다** | [push-fcm/summary.md](./features/push-fcm/summary.md) | backend | `NotificationDispatcherWiringTest`가 리플렉션으로 **어노테이션 존재·값만** 고정한다. 막는 실패 모드는 *"누가 어노테이션을 지우거나 phase/propagation을 바꾼다"* 하나이고, **"정말 커밋 후에 도는가"는 증명하지 않는다.** 계약 §0.3에 *"`REQUIRES_NEW`를 빠뜨리면 저장이 예외 없이 조용히 사라진다"*고 못 박아뒀는데 그게 실제로 안 사라지는지는 미확인이다. → **통합 테스트 필요**(컨테이너 런타임 종속). 테스트 KDoc이 한계를 스스로 적어둔 것은 좋으나 한계인 건 변하지 않는다. |
| 🔴 **§0.2 토큰 소유권 이전 SQL의 자동 검증 부재** | [push-fcm/api-contract.md §0.2](./features/push-fcm/api-contract.md) | backend | ✅ **2026-08-07 실사용으로는 증명됐다**(계정 전환 시 토큰 15→16 이동, 15는 NULL). 그러나 **자동 테스트는 fake만 태운다** — `UserServiceFcmTokenTest`의 fake가 `store.values.filter { it.fcmToken == token && it.id != exceptUserId }` 로 **SQL 술어를 Kotlin으로 다시 쓴 것**이라 실제 `AND u.id <> :exceptUserId`를 검증하지 않는다. fake와 SQL이 갈라져도 초록이다. **회귀를 잡으려면 통합 테스트가 필요**하다(컨테이너 런타임 종속). |
| ~~🟢 **알림 목록 화면 미구현**~~ ✅ **2026-09-01 해소 — feature `notification-list`** (조회 API 3종 + 화면 + 홈 벨 뱃지 점등). ⚠️ ~~양 레포 미커밋이라 아직 `implemented` 다~~ → ✅ **2026-09-05 정정: 양 레포 커밋됨**(app `b5e7233` / server `bc18aea`). `implemented` 에 머무는 사유는 이제 **실기 미확인** 하나다 | [push-fcm/summary.md](./features/push-fcm/summary.md) | backend + mobile + design | `notifications` row는 `push-fcm`부터 쌓이기 시작했다(현재 8건). Lovable `notifications.tsx`는 이미 존재. ⚠️ 기획서 §4 IA상 **알림 화면에 도달할 진입 경로가 홈 상단 벨 아이콘**으로 정의돼 있는데 아직 없다. 🔵 **2026-09-01 결과**: 커서 페이지네이션을 프로젝트 최초 도입([ADR-0012](./decisions/0012-list-pagination-cursor.md)) · 읽음 처리 (B) + 뱃지 점등 · **앱 `PlaceholderScreen` 소비처 0건 달성**(기획서 §4 IA 의 진입 경로 중 빈 화면으로 끝나는 것이 안 남았다). [summary](./features/notification-list/summary.md) |
| 🟢 **알림 딥링크 미구현** (알림 탭 → 챌린지 상세) | [push-fcm/summary.md](./features/push-fcm/summary.md) | mobile | 현재 탭하면 홈까지만 간다. 도전장이 여러 개면 어느 것인지 알 수 없어 알림 가치가 반감된다. ✅ **`data.challengeId`는 이미 페이로드에 실려 있어** 후속에서 라우팅만 붙이면 **서버 변경 0**이다(계약 §0.5 의도). Navigation 3 딥링크 배선이 실제 작업. |
| 🟢 **`onNewToken` 등록이 `scope.cancel()`에 잘릴 수 있다** | [push-fcm/summary.md](./features/push-fcm/summary.md) | mobile | `onNewToken` 반환 ≠ launch한 코루틴 완료다. 서비스가 `stopSelf` → `onDestroy` → `scope.cancel()`로 **네트워크 왕복 중인 등록을 죽일 수 있다.** ⚠️ **관측된 적 없고 자가 치유된다**(다음 로그인 시 재등록). 발화 빈도도 낮다(재설치·데이터 삭제·로테이션). **조치하지 않기로 결정**(사용자 원칙: 일어나지 않은 일에 방어 로직 추가 금지) — 다만 *"토큰 갱신 후 푸시가 안 온다"* 증상의 **첫 번째 용의자**로 기록해 둔다. |
| 🟢 **알림 아이콘이 디자인 미확정 placeholder** | [push-fcm/summary.md](./features/push-fcm/summary.md) | design + mobile | `ic_notification.xml` 임시 종 모양. Android 규칙(흰색 실루엣 + 투명 배경)은 지켰으나 **실제 트레이 렌더 미확인.** 디자이너 자산 나오면 파일만 교체. |
| 🟡 **알림 문구 확정 — 🟡 초안 3건 + 확정 1건, 4종 일괄 확정 필요** | [push-fcm/api-contract.md §3](./features/push-fcm/api-contract.md) · [challenge-verification/summary.md](./features/challenge-verification/summary.md) | user + design + pm | 초안: `CHALLENGE_ACCEPTED` / `CHALLENGE_REJECTED` / **`OPPONENT_VERIFIED`("증거 도착")**. 확정: `CHALLENGE_REQUEST`. `NotificationMessages` KDoc 이 *"임의 통일 금지, 확정분을 초안에 맞추지 마라"* 로 잠가 뒀다. 톤 불일치(격식/구어 혼재)도 이때 함께. ✅ 문구는 한 곳에만 있어 **모바일 코드 변경 없이** 바꿀 수 있다. |
| 🟢 **`./scripts/create-core-module.sh` 패키지 경로 버그 — `generate-feature.sh`와 같은 건이 안 고쳐진 채 남아 있다** | 2026-08-06 `push-fcm` T-M1c `:core:push` 신설 중 발견 (mobile-dev) | mobile | `PACKAGE_PATH="com/lwg/base/$MODULE_NAME"` 라서 `com/lwg/base/push` 를 만든다. **이 레포에 `com.lwg.base` 는 한 곳도 없다** — 실제 컨벤션은 `com.lwg.challenge.*` 다. 🔴 **자매 스크립트 `generate-feature.sh` 의 동일 버그는 2026-06-15에 이미 고쳤는데**(line 35 `com.lwg.base` → `com.lwg.challenge`) **`create-core-module.sh` 는 빠뜨렸다.** 이번엔 mobile-dev 가 세 소스셋을 손으로 다시 만들고 `com/lwg/base` 트리를 지워 우회했다(잔존 0건 확인). 부수 문제 하나 더 — `include(":core:push")` 를 파일 맨 끝에 `>>` append 해서 `// Core` 섹션 밖으로 나간다(이번엔 수동으로 옮김). **다음에 core 모듈 만드는 사람이 같은 데 걸린다.** |
| 🟢 **3개 모듈의 iOS 테스트가 한 번도 돈 적이 없다** (`:remote:datasource` · `:data:repositoryImpl` · `:core:utils`) | 2026-08-18 `challenge-verification` iOS 검증 중 발견 ([mobile-report.md](./features/challenge-verification/mobile-report.md) 미해결 15번) | mobile | **Kotlin/Native 가 백틱 함수명의 `,` `(` `)` 를 금지**하는데(JVM 은 허용) 이 세 모듈 commonTest 에 위반이 **22건/5파일** 남아 있어 iOS 테스트가 **컴파일 자체가 안 된다.** 단위 테스트가 JVM 에서만 검증되는 상태 — 특히 `:remote:datasource` 는 네트워크 경계라 플랫폼 차이가 날 수 있는 자리다. 증거: baseline 에 iOS 테스트 XML 이 존재하는 모듈(`core:push`/`feature:main`/`core:camera`)은 위반 0건, 위반 있는 모듈은 정확히 XML 부재. **해소 비용 낮음** — 이름 22개 rename, 로직 무변경 (`challenge-verification` 이 자기 몫 1건은 이미 수정: `onError 호출, onSuccess 미호출` → `onError 호출하고 onSuccess 미호출` 패턴). ⚠️ 신규 테스트 작성 시 백틱 이름에 `,`/`(`/`)` 금지 — child claude 가 기존 파일 관행을 답습해 재발한 전례가 이번 건이다. 🔴 **2026-08-28 실측 보강(보관함 월 개정)**: 손실 규모가 **89건**으로 확인됐다(`:remote:datasource` 39 · `:core:utils` 35 · `:data:repositoryImpl` 15 — 전부 Android 에서만 돈다). ⚠️ **리포트가 이걸 "iOS —"(테스트 없음)로 적어 온 것이 오독이었다** — 있는데 안 돌고 있었고 숫자만 봐서는 알 수 없이 조용히 빠진다. 별건으로 `:local:database:kspKotlinIosSimulatorArm64` 도 실패한다. 🔴 **2026-09-01 부분 해소 + 숫자 관계 명확화**(notification-list T-M3). `:core:utils` **완료** — 위반이 **함수명 1개**뿐이었고(`WireFormatBaselineTest.kt:32` `옛 방식(Instant 차)`) rename 1건으로 **iOS 50/50 개통**(그 모듈 `iosSimulatorArm64Test` 디렉터리가 처음 생겼다 = 한 번도 안 돌았다는 증거). 남은 위반 **22 → 22건**(`:remote:datasource` **14** · `:data:repositoryImpl` **8** · `:core:utils` **0**) / 남은 손실 **89 → 54건**(39 · 15 · 0) — pm 이 백틱 **안쪽** 문자만 세어 독립 검증했고 mobile 실측과 일치한다. 🔴 **이 항목의 핵심은 비용과 편익의 단위가 다르다는 것이다** — 비용은 **이름 1개 단위**인데 편익은 **모듈 1개 단위**다. 한 파일의 이름 하나가 모듈 전체 컴파일을 막으므로 ROI 가 극단적으로 불균등하다(`:core:utils` 는 1 : 35 였다). 착수 시 **위반 수가 적고 테스트 수가 많은 모듈부터** 잡아라. ⚠️ **세는 법 함정**(mobile 이 처음에 밟았다): `grep` 로 백틱 함수를 뽑아 `[,()]` 를 다시 거르면 로 세면 시그니처의 `()` 때문에 **모든 백틱 함수가 걸려** 위반 수가 손실 수와 같아진다. **백틱 안쪽만 봐야 한다.** ⚠️ 위 두 숫자(22 = rename 비용 / 89 = 손실 커버리지)는 원래도 구분해 적혀 있었으나 **89 에만 모듈별 내역이 있어** 읽는 쪽이 그걸 위반 수로 오인했다 — 이번 갱신에서 양쪽 모두에 내역을 붙였다. |
| ~~🟡 `LogoutUseCase`가 서버 `DELETE /auth/logout`을 호출하지 않는다~~ ✅ **2026-08-26 해소 (mypage T-M2)** — 서버 호출 실구현. 선결이던 `WWW-Authenticate` 실측(헤더 없음·없어도 갱신됨)과 필수 사실 2건(clearTokens 앞 호출·비중복) 전부 준수, `sendWithoutRequest` 를 auth 3경로 allowlist 로 축소(기존 401 갱신 8건 회귀 0). 부수 발견: 구 술어에서는 logout 이 401 을 받아도 자동 갱신조차 안 돌았다 | [push-fcm/spec.md 수용 기준](./features/push-fcm/spec.md) · [api-contract.md 오픈 이슈 4](./features/push-fcm/api-contract.md) (2026-08-06 pm-lead 결정으로 push-fcm 범위 제외) | mobile | 로컬 토큰 정리만 하고 서버를 안 부른다. 서버 T-B2는 **구현·검증 완료**라 엔드포인트는 있는데 **아무도 부르지 않는 상태**다. → 로그아웃해도 서버 `fcm_token`이 남아 **그 기기로 알림이 계속 간다.** ⚠️ **가정이 아니라 미구현 기능** — `push-fcm` 발송이 붙는 순간 확정적으로 발생한다. 다만 §0.2 토큰 소유권 이전이 *계정 전환*은 덮으므로 **실제 공백은 "로그아웃하고 아무도 로그인하지 않은 기기" 하나**다. 🔴 **한 줄 추가가 아니다** — `KtorfitModule.kt:103`의 `sendWithoutRequest { pathSegments.none { it == "auth" } }` 가 `/auth/logout`(Bearer 필수)까지 걸어버려 술어를 좁혀야 하고, 이는 `auth-refresh-rotation`이 401 처리를 모아둔 **인증 플러그인 전역 동작 변경**이다. 🔴 **재개 전 선결: `WWW-Authenticate` 실측.** `UnauthorizedEntryPoint`가 그 헤더를 안 보내서 401 후 Ktor 재시도가 도는지 자체가 미검증이고, 그게 술어 범위 결정의 입력이다. **구현 시 필수 사실 2건**: ① 서버 호출은 **`clearTokens()` 앞** — 뒤에 두면 토큰이 없어 401→refresh 실패로 **정상적인 테스트 계정 전환이 "세션 만료"로 반응**한다. ② **순수 중복 아님** — logout은 소유권 이전이 안 건드리는 `refresh_token_hash`도 끊는다. |
| 🟡 **iOS Bundle ID가 `$(TEAM_ID)`에 묶여 있다 — Apple 계정 구입 시 값이 바뀐다** | 2026-08-06 `push-fcm` 착수 중 실측 (`iosApp/Configuration/Config.xcconfig:4`) | mobile + user | `PRODUCT_BUNDLE_IDENTIFIER=com.lwg.challenge.Challenge$(TEAM_ID)` 인데 `TEAM_ID=` 가 **비어 있다.** 현재는 `com.lwg.challenge.Challenge` 로 해석되지만, **Apple Developer 계정을 사서 `TEAM_ID` 를 채우는 순간 Bundle ID 가 바뀐다** (KMP 위저드가 무료 프로비저닝 충돌을 피하려고 넣는 관례). 🔴 **후폭풍이 두 갈래다** — (1) Firebase iOS 앱은 Bundle ID 로 묶이므로 미리 등록해두면 `GoogleService-Info.plist` 불일치로 **초기화가 거부**된다(그래서 `push-fcm` 에서 iOS 선등록을 보류했다). (2) **카카오 네이티브 앱 키가 iOS Bundle ID 에 묶여 있어 콘솔을 함께 갱신하지 않으면 카카오 로그인이 깨진다.** → 실배포 전에 **접미사를 떼고 고정 Bundle ID 를 확정**한 뒤 카카오 콘솔을 갱신하는 순서로 처리. 현재 `.entitlements` 파일 0건(Push capability 미적용)이 **정상 상태**이므로 건드리지 말 것 — 무료 계정으로 켜면 서명이 실패해 실기 빌드가 깨진다. |
| **friends iOS 카톡 공유 실제 연동** (iosApp SPM에 `KakaoSDKShare` 추가 + Swift에서 `KakaoInviteBridge.inviteHandler` 주입) | [friends/mobile-report.md](./features/friends/mobile-report.md) | mobile + user | 현재 iOS는 placeholder(handler null→failure). `KakaoLoginHelper.swift` 패턴 답습. Xcode 작업 필요. |
| **friends iOS 유닛테스트** (`:feature:friends:iosSimulatorArm64Test`) + iOS framework link | [friends/mobile-report.md](./features/friends/mobile-report.md) | mobile | T7b 검증은 Android 유닛 + iOS 컴파일까지. iOS 유닛 별도 실행. |
| **`profileImageUrl` 화면 적용** (친구 목록/검색/내 프로필의 이니셜 placeholder 활성화) | [friends/mobile-report.md](./features/friends/mobile-report.md) → [challenge-verification/summary.md](./features/challenge-verification/summary.md) | mobile + design | ✅ **로더 도입 자체는 2026-08-25 해소** — challenge-verification T-M4 가 Coil 3 를 기본 인증 클라이언트로 배선(인증 사진이 첫 원격 이미지). 🔴 **2026-08-26 차단 사유 전면 갱신(rank-backend·rank-design 실측)** — "Coil 배선 0건"은 더 이상 사실이 아니다(challenge-verification 이 배선 완료, `SubcomposeAsyncImage` 실사용 중). 그리고 카카오 실계정의 `profileImageUrl` 에 **실 CDN URL 이 온다**("항상 null" 아니었음). **그런데 지금 켜면 iOS 에서만 깨진다**: URL 이 `http://img1.kakaocdn.net/...` 평문인데 Android 는 cleartext 전 호스트 허용, **iOS 는 ATS 가 도메인명 평문을 차단**(Info.plist 주석의 "숫자 IP 는 안 막는다" 전제가 CDN 도메인에는 성립 안 함). **새 선행 조건 2개**: ① 🔴 **서버 https 정규화** — 카카오 URL 을 https 로(앱 ATS 예외 방향은 기각 — 전 호스트 평문을 iOS 에 여는 것 + 심사 축). ✅ **2026-08-26 rank-backend 사실 검증 완료, 서버 단독 소품 확정**: https 로 동일 서빙됨(200/9203B 실측, 리다이렉트 없음 — 스킴만 교체), 유입 **1곳**(`AuthService` 카카오 응답 저장)·노출 6곳이라 한 곳 수정으로 전 응답 해결, 기존 row 마이그레이션 **1행**뿐. 🔴 **구현 함정**: URL 에 `http` 가 두 번 나온다 — `fname=http%3A%2F%2F...` 쿼리 파라미터는 카카오 썸네일러의 서버측 원본 주소라 **바꾸면 안 된다**(ATS 무관). `.replace("http://","https://")` 전체 치환은 버그 — **스킴 접두사 하나만** 교체. 아바타 활성화와 분리 가능 — 먼저 해 두면 iOS 함정이 미리 사라진다. ② `ProfilePlaceholder` 가 4개 feature 공용이라 단독 활성화 불가(회귀 표면 — challenge-verification spec §0.4 논거 유효). loser-ranking 은 이니셜로 확정, 랭킹 화면도 활성화 대상에 포함. |
| **`IconTextButton iconSize` 파라미터화** (design.md 14dp vs 공용 컴포넌트 16dp) | [friends/mobile-report.md](./features/friends/mobile-report.md) | mobile | 2dp 차이 정정 원할 때. `:core:designsystem/IconTextButton.kt`에 `iconSize: Dp = 16.dp` 파라미터 추가. |
| **friends 초대 manual smoke** (Android 카톡 공유 시트 정상 열림 + Default TextTemplate 렌더 확인) | [friends/summary.md](./features/friends/summary.md) | user | 모바일 commit/push 후 디바이스에서 확인. |
| **friends 모바일 working tree commit/push** (T6+T7a+T7b 누적, 신규 ~19 + 수정 ~12) | [friends/mobile-report.md](./features/friends/mobile-report.md) | user | mobile-dev git 금지 룰로 working tree만 변경됨. 한 commit / 분리 사용자 결정. |
| **로컬 저장 모델 스키마 드리프트** (`TokenPrefs` / `UserInfoPrefs` / `AppSettingsPrefs`) | [soul-oath/change-log.md](./features/soul-oath/change-log.md) (#25 감사, mobile-dev 지적) | mobile | DataStore 영속화 모델이라 **wire 계약과 축이 다르다** — wire는 서버·클라가 같은 시점에 배포되지만 로컬 저장은 **앱 버전 간** 드리프트라 해법 성격이 다르다(마이그레이션/버저닝). #25 감사에 끼워 넣으면 둘 다 얕아져서 분리했다. |
| user-info iOS 단위 테스트 + framework link 검증 (`:feature:home:iosSimulatorArm64Test` + `xcodebuild`) | [user-info/summary.md](./features/user-info/summary.md) | mobile + user | T4 dispatch가 Android testDebugUnitTest만 수행. iOS 측 검증 별도 필요. |
| 내 프로필 화면 (UserInfo 활용 — 닉네임 / 프사 노출 + 친구 초대 시 사용) | [user-info/summary.md](./features/user-info/summary.md) | design + mobile | 본 작업에선 캐시만 채우고 UI 노출 없음 (HomeScreen.kt 미수정). 디자인 + Lovable 작업 후 mobile 통합. |
| user-info 명시적 갱신 UX (pull-to-refresh / 카카오 프로필 변경 감지) | [user-info/summary.md](./features/user-info/summary.md) | mobile | 1차에 `NETWORK_ONLY` 호출 지점 없음. 사용자 트리거 추가 시 호출. |
| user-info manual smoke (신규 가입 / 자동 로그인 / 로그아웃 캐시 클리어 디바이스 검증) | [user-info/summary.md](./features/user-info/summary.md) | user | 모바일 commit/push 후 디바이스에서 확인. |
| user-info 모바일 commit/push 결정 (신규 12 + 수정 10 + 삭제 1) | [user-info/mobile-report.md](./features/user-info/mobile-report.md) | user | mobile-dev git 금지 룰로 working tree만 변경됨. 한 commit / 분리 사용자 결정. |
| `SilentAuthExpired` 공통 위치 이동 (`:domain:model/auth/`) — `faae2cd`로 삭제된 sentinel 재도입 검토 | [user-info/summary.md](./features/user-info/summary.md) | mobile | 본 작업에선 사용 안 함 (옵션 1 표준 따름). 필요해지면 별도 spec. |
| ~~`TokenLocalDataSource` interface 분리~~ / `AppSettingsLocalDataSource` interface 분리 (테스트 추가 시) | [user-info/mobile-report.md](./features/user-info/mobile-report.md) · [dev-test-login](./features/dev-test-login/spec.md) | mobile | `TokenLocalDataSource`는 ✅ **2026-07-31 `dev-test-login`에서 해소** — "테스트 추가 시" 조건 충족(final concrete + 생성자에서 `createDataStore` 즉시 실행이라 `LoginRepositoryImpl` 테스트가 **한 건도 불가능**했다). `UserInfoLocalDataSource` 패턴 답습, 사용처 2곳 타입명 유지로 무변경. **`AppSettingsLocalDataSource`는 미해소** — 동일 조건 충족 시 검토. |
| AuthKakao 통합 테스트 수동 실행 (Docker Desktop 후 `--tests "*AuthKakaoIntegrationTest"`) | [auth-kakao/backend-report.md](./features/auth-kakao/backend-report.md) | user + backend | 5 케이스 검증 (drift 정정 후 5xx 1회 재시도→703 시나리오 추가). CI 적용 시 docker-in-docker 필요. |
| 로컬 서버 기동 + 모바일 수동 smoke test (Android+iOS real Kakao SDK 흐름) | [auth-kakao/summary.md](./features/auth-kakao/summary.md) | user | NATIVE APP KEY 기입 완료. Android 에뮬레이터/실기기 + iOS Simulator/Device에서 카카오 로그인 → JWT 발급 E2E 확인 필요. 카카오톡 앱-간 인증과 웹 fallback 둘 다. |
| iOS SPM 카카오 SDK 버전 pin 적정성 확인 | [auth-kakao/change-log.md](./features/auth-kakao/change-log.md) | mobile | `iosApp/iosApp.xcodeproj/project.pbxproj`의 `XCRemoteSwiftPackageReference`에 `requirement`(`upToNextMajor` 등) 명시 여부 점검. |
| `:feature:login:check` detekt config 부재 (`/config/detekt/detekt.yml`) | [auth-kakao/change-log.md](./features/auth-kakao/change-log.md) | mobile | 컴파일/테스트는 통과하나 `check` 태스크 fail. 인프라 티켓. |
| iOS Keychain 실기기 smoke (write/read roundtrip) | [auth-kakao/mobile-report.md](./features/auth-kakao/mobile-report.md) | mobile | SecItemAdd/Copy 상태코드 확인. |
| Compose blur 미적용 (LoginScreen `blur-3xl` + bottom-navigation `backdrop-blur-xl` 동일 카테고리) | [auth-kakao/mobile-report.md](./features/auth-kakao/mobile-report.md) · [bottom-navigation/summary.md](./features/bottom-navigation/summary.md) | mobile + design | 현재 alpha만으로 근사. 시각 차이 크면 platform actual blur(`graphicsLayer { renderEffect = BlurEffect(...) }` Android API 31+ / iOS `UIVisualEffectView` interop) 도입 검토. KMP 공통 API 부재로 expect/actual 분기 필요. |
| Material Icons Extended 1.7.3 deprecation | [auth-kakao/mobile-report.md](./features/auth-kakao/mobile-report.md) · [bottom-navigation/summary.md](./features/bottom-navigation/summary.md) | mobile + pm | Kotlin 2.2.20 / CMP 1.10.3 호환 이슈로 1.7.3 핀. `:feature:login`(`LocalFireDepartment`/`AutoAwesome`)에 이어 `:feature:main` BottomBar(`Home`/`Group`/`EmojiEvents`/`Person`)도 동일 의존성 사용 시작 — 사용처 누적. Material Symbols(vector resources) 마이그레이션 ADR 후보. |
| Glow shadow / 외광 효과 미구현 | [auth-kakao/mobile-report.md](./features/auth-kakao/mobile-report.md) | mobile + design | tokens.md §3 `--shadow-glow` (0 0 30px primary 20%) Compose 기본 `shadow()` 표현 어려움. Canvas glow 별도 작업 후보. |
| 카카오 버튼 ripple 색 (`KakaoYellowPressed`) | [auth-kakao/design.md](./features/auth-kakao/design.md) | mobile + design | 현재 Surface 기본 ripple. 디자이너 확인 후 `BrandColors.KakaoYellowPressed` ripple 매핑 작업. |
| ~~`./scripts/generate-feature.sh` 패키지 경로 버그~~ | ~~[bottom-navigation/summary.md](./features/bottom-navigation/summary.md)~~ | ~~mobile~~ | ✅ 2026-06-15 — line 35 `com.lwg.base` → `com.lwg.challenge` 정정. 모바일 working tree 변경, 사용자 커밋 대기. |
| `:feature:login` Preview annotation deprecation 경고 다수 | [bottom-navigation/summary.md](./features/bottom-navigation/summary.md) | mobile | bottom-navigation 빌드 중 발견. `@Preview` 사용처 일괄 마이그레이션 (Material3 / KMP Preview annotation 변경). 정리 후보, 즉시 가능. |
| LoginScreen 10sp 라벨 `bold10`/`light10` 슬롯 정합 | [tokens.md §5.2](./design-system/tokens.md) · [auth-kakao/design.md](./features/auth-kakao/design.md) | mobile + design | 2026-05-12 design-bridge Lovable 전수 점검 결과. Lovable `login.tsx`: LabeledDivider "한 번 서명하면 무를 수 없음" `text-[10px] font-bold`(10sp) / 약관 풋터 `text-[10px]`(10sp, weight 미지정→Light). 현 모바일은 `bold12`/`light12`(12sp). (1) `:core:designsystem/ChallengeTypography.kt`에 `bold10`/`light10` 슬롯 추가(`medium10`과 동일 패턴, lineHeight 14). (2) LoginButtonSection의 LabeledDivider `textStyle = typography.bold10`, FooterAgreementText `style = typography.light10`로 교체. ranking/index/notifications/challenge-detail/challenge-new/ChallengeCard 등 다른 화면은 후속 feature 진입 시 자연 정합(현재는 모바일 placeholder). |
| ~~`/actuator/health`가 HTTP 500~~ ✅ **2026-08-03 해소** (`dev-test-login` T-B1b의 `NoResourceFoundException` → 404 핸들러. 실서버 404 확인) | [challenge-create/backend-report.md](./features/challenge-create/backend-report.md) | backend | 2026-07-31 #7 통합 검증 중 발견. `SecurityConfig`가 permitAll로 열어 뒀으나 **actuator 의존성이 없어 핸들러가 없다.** 현재 사용처 없어 영향 0. 헬스체크·로드밸런서 프로브로 쓰려면 `spring-boot-starter-actuator` 추가 필요. ADR-0007 AWS 배포 시 실제 필요해진다. |
| 동시 로그인 시 `kakao_id` UNIQUE 충돌 재시도 로직 | auth-kakao/backend-report.md | backend | MVP 현실성 낮음. 향후 보강. |
| `AuthServiceRefreshTest` 단위 테스트 (정상 rotation / 옛 refresh 재사용 / hash NULL / JWT 만료 / tokenType 잘못 — 5 케이스) | [auth-refresh-rotation/summary.md](./features/auth-refresh-rotation/summary.md) | backend | 본 PR에서 미작성. JwtTokenProvider + UserRepository mock 기반 단위 테스트로 충분. |
| Ktor `Auth(bearer)` 플러그인 모바일 테스트 (200 분기 / 401 분기 / refresh JSON deserialization / 다중 요청 race 처리) | [auth-refresh-rotation/summary.md](./features/auth-refresh-rotation/summary.md) | mobile | Ktor MockEngine + 가짜 TokenProvider/AuthEventBus로 검증. |
| Refresh rotation E2E smoke (access 만료 시뮬 + V1 가입자 강제 재로그인) | [auth-refresh-rotation/summary.md](./features/auth-refresh-rotation/summary.md) | user + backend + mobile | `application.yml` `jwt.access.expire` 짧게 → Android/iOS 모바일에서 (1) 사용자 무자각 갱신 (2) refresh 만료 시 로그인 화면 자동 이동 (3) V1 hash NULL row 강제 재로그인 확인. |
| ~~logout 엔드포인트 구현 (`/auth/logout`)~~ | ~~foundation Sprint 0 스텁 / ADR-0009~~ | ~~backend~~ | ✅ **2026-08-07 `push-fcm` T-B2로 해소** — `refresh_token_hash` + `fcm_token` 이중 NULL화, 멱등. foundation 시절부터 남아 있던 미구현 TODO를 흡수했다. ⚠️ **다만 모바일이 아직 이 엔드포인트를 호출하지 않는다** — 위 🟡 `LogoutUseCase` 항목 참조. |
| Refresh rotation race 동시성 분석 | [auth-refresh-rotation/summary.md](./features/auth-refresh-rotation/summary.md) | backend + mobile | 동일 refresh 2 요청 동시 도착 시 한쪽 401 실패의 UX 영향. Ktor Auth 단일 클라이언트 직렬화로 단일 기기에선 발생 빈도 낮음 — 다중 기기 동시 갱신은 1기기 1세션 정책의 자연 귀결. 관측되면 별도 ADR. |
| ~~`challenge-app/CLAUDE.md` 모듈 구조 정정~~ | ~~[ADR-0003](./decisions/0003-mobile-template-init.md)~~ | ~~mobile~~ | ✅ 2026-06-15 — `:domain:*`/`:remote:*`/`:data:*`/`:local:*` 다중 모듈 구조 + `com.lwg.challenge` ComponentScan 예제로 정정. **ADR-0003 6/6 항목 처리 완료** → accepted 전환 가능. 모바일 working tree 변경, 사용자 커밋 대기. |
| RecordApi/ActiveChallengeApi 통합 테스트 6건 수동 실행 (Docker Desktop 후 `--tests "*RecordApiIntegrationTest" --tests "*ActiveChallengeApiIntegrationTest"`) | [home-feed/change-log.md](./features/home-feed/change-log.md) | user + backend | v2에서 `HomeIntegrationTest`가 `RecordApiIntegrationTest`(3) + `ActiveChallengeApiIntegrationTest`(3)로 분리됨. docker 미설치 자동 skip 패턴 유지. |
| 모바일 시각 검증 (HomeScreen 3 상태 + ChallengeCard + StatsBar muted 톤) | [home-feed/summary.md](./features/home-feed/summary.md) | user | Android 에뮬레이터 + iOS Simulator. default/FIRST_USER/NO_ACTIVE_CHALLENGE 3 상태, 7필드 카드, StatsBar 신규사용자 muted 톤, FAB pulse-fire 부재, safe-area inset 확인. 백엔드 통합 verify 후 권장. |
| 디자이너 결정 4건 (home-feed) — FIRST_USER FAB 노출 / NO_ACTIVE_CHALLENGE CTA 1버튼 vs 2버튼 / 빈 상태 일러스트 자산 / `Swords`↔`SportsKabaddi` 시각 차이 | [home-feed/design.md §7](./features/home-feed/design.md) | design + user | 본안 그대로 구현됨. 디자이너 결정 시 모바일 1줄 수정 또는 props 변형으로 대응. |
| FAB `pulse-fire` 애니메이션 미구현 (HomeScreen) | [home-feed/summary.md](./features/home-feed/summary.md) · 2026-08-26 loser-ranking design.md 재정의 | mobile + design | 🔴 **문구 정정(2026-08-26)**: ~~"LoginScreen pulse-fire 패턴 재사용 후보"~~ — **LoginScreen 패턴(`SoulStampLogo`)은 glow 가 아니라 alpha 호흡**이다. `pulse-fire` 의 CSS 정의(box-shadow glow)는 **보류 확정**(tokens.md §3 유보 + loser-ranking design.md 판정: "transform·alpha 구현 / box-shadow glow 보류"). 모션을 원하면 **alpha 호흡으로 근사**한다 — Canvas glow 를 만들러 가지 말 것. |
| ChallengeCard 시간 표시 자동 갱신 부재 | [home-feed/summary.md](./features/home-feed/summary.md) | mobile | 현재 매 composition 재계산만. `LaunchedEffect + delay(60s)` 분 단위 재구성 후보. |
| Lovable "deadline 완료" 케이스 정의 (IN_PROGRESS인데 deadline 지남) | [home-feed/summary.md](./features/home-feed/summary.md) | pm + backend + design | 1차 API 응답에선 미포함, ChallengeCard는 `DISTANT_PAST` 폴백으로 "마감" 표시. 어떤 시점에 COMPLETED로 전환할지 챌린지 결과 판정 feature에서 결정. |
| **`challengeKotlinMultiplatformPure` 모듈의 `api()` 의존성이 Android 타깃으로 전이되지 않음** | [datetime-model-migration/mobile-report.md](./features/datetime-model-migration/mobile-report.md) | mobile | `:domain:model`이 `api(kotlinx-datetime)`을 선언해도 android 타깃이 없는 모듈이라 변형 매칭에서 api 간선이 유실된다. 현재는 feature 3곳에 `implementation` **명시 선언**으로 우회(레포의 `materialIconsExtended` 선례와 동일 대응). 근본 해결은 build-logic 컨벤션 플러그인 수정. |
| **위저드 시스템 백 버튼 배선 미완** (~~`:core:ui` 승격~~ → **승격은 완료**, 위저드 연결만 남음) | [challenge-create/summary.md](./features/challenge-create/summary.md) · [soul-oath](./features/soul-oath/spec.md) | mobile | ⚠️ **2026-08-03 사유 정정** — 원래 문구였던 *"`PlatformBackHandler`가 `:feature:main`에 갇혀 못 쓴다"*는 **더 이상 사실이 아니다.** `soul-oath`에서 `:core:ui`로 승격됐고(맹세 화면이 두 번째 소비자) `OathRoute.kt:33`이 실제로 쓴다. **남은 건 위저드 배선뿐.** 의도적으로 미착수 — 위저드는 시스템 백이 **3단계 상태기계**(OATH → MISSION_INPUT → FRIEND_PICK → pop)를 거슬러야 하고, 맹세 step엔 서명이 있어 **dirty 이탈 확인까지 얽힌다.** 대충 붙이면 TopBar 백과 시스템 백이 다르게 동작한다. `ChallengeCreateRoute.kt:41` 주석도 정정돼 있다. |
| **challenge-create 실기기 시각 검증** (위저드 2-step / 받은 도전장 섹션 / **수락 다이얼로그 iOS IME**) | [challenge-create/summary.md](./features/challenge-create/summary.md) | user + mobile | 다이얼로그를 택한 이유가 바텀시트의 iOS IME 리스크였던 만큼 다이얼로그 쪽도 실기 확인이 필요하다. ⚠️ 실기기 첫 네트워크 호출 시 **로컬 네트워크 권한 팝업 "허용"** 필수 — 거부하면 Apple 버그 r.131764908로 기기 재시작 전까지 캐시된 거부가 유지된다. |
| challenge-create iOS 유닛 테스트 미실행 (`:feature:home` / `:feature:challenge:create` `iosSimulatorArm64Test`) | [challenge-create/summary.md](./features/challenge-create/summary.md) | mobile | Android 유닛 + iOS 링크까지가 검증 게이트였다. friends·user-info와 동일 패턴의 누적 항목. |
| `SearchProfilePlaceholder` 4번째 사본 흡수 (`:feature:friends:search`) | [challenge-create/mobile-report.md](./features/challenge-create/mobile-report.md) | mobile | challenge-create에서 `ProfilePlaceholder`에 `size` 파라미터를 넣어 시각 변화 없이 흡수 가능. 완료된 feature 영역이라 손대지 않음. |
| `EXPIRED` 전이 주체 없음 (`:batch` 스케줄러) | [challenge-create/summary.md](./features/challenge-create/summary.md) | backend | lazy expiry로 처리 중 — 목록 제외 + 수락 거부. DB `status`는 `PENDING`인 채 남는다. |
| ~~`verifications` PENDING row 자동 생성 로직 (챌린지 IN_PROGRESS 진입 시)~~ | ~~[home-feed/backend-report.md](./features/home-feed/backend-report.md)~~ | ~~backend~~ | ✅ **2026-07-31 challenge-create로 해소** — 수락 시 양측 2건을 단일 트랜잭션으로 생성. |
| **401 응답의 `WWW-Authenticate` 헤더 부재 — RFC 9110 §15.5.2 위반 (동작 문제 아님)** | 2026-08-26 mypage 실측 ([api-contract §3](./features/mypage/api-contract.md)) | backend | 동작상 불필요가 실측 증명됨 — Ktor Auth 는 프로바이더 1개면 헤더 없어도 갱신하며, 그 전제가 깨지는 순간 터지는 특성화 테스트 2건이 앱에 박제돼 있다. 규격 정합만 남은 건 — 서버에 프로바이더 추가·표준 클라이언트 대응이 필요해지는 시점에 `UnauthorizedEntryPoint` 한 줄. |
| ~~user_stats 자동 집계 트리거 (챌린지 결과 확정 시)~~ ✅ **2026-08-25 해소** — challenge-result 판정 트랜잭션이 user_stats+friend_records 집계. 홈 전적 첫 실데이터化 | [home-feed/backend-report.md](./features/home-feed/backend-report.md) → [challenge-result/summary.md](./features/challenge-result/summary.md) | backend | LEFT JOIN 0 응답 폴백은 신규 유저용으로 여전히 유효. |
| 챌린지 deadline UTC 일관성 명시 (생성 feature 진입 시) | [home-feed/backend-report.md](./features/home-feed/backend-report.md) | backend + pm | 현재 DB TIMESTAMP를 UTC 가정. 챌린지 생성 feature에서 저장 정책 명문화 필요. |
| friends 1차 2단계 — 친구 목록 백엔드 (`friendships` 테이블 V5 + `GET /api/v1/friends` + 통합 테스트) | [friends/summary.md](./features/friends/summary.md) | backend | spec.md "후속 계획" 참조. 양방향 단일 row(`user_a_id < user_b_id`) + users JOIN 1쿼리. |
| friends 1차 2단계 — 모바일 친구 목록 (`Friend` 도메인 + Repository/UseCase + `FriendCard` + LazyColumn 렌더링) | [friends/summary.md](./features/friends/summary.md) | mobile | Flow + onError + AuthEventBus 표준 패턴. `FriendsUiState.Data` 에 `friends: List<Friend>` 필드 + `isEmpty` derived. |
| friends 시각 검증 (Android 에뮬레이터 + iOS Simulator + Lovable 프리뷰 3방향) | [friends/summary.md](./features/friends/summary.md) | user + design | 빈 상태 일러스트(현재 `Icons.Filled.Group` 임시) / 헤드라인 톤 / CTA 시각. design.md §6 ⚠️ 확인 필요 6건 점검. |
| friends 빈 상태 일러스트 자산 (1차 2단계 진입 전) | [friends/design.md](./features/friends/design.md) | design | 현재 `Icons.Filled.Group` 임시. 디자이너 자산 추가 시 `FriendsEmptyState` 호출 인자만 교체. |
| friends 친구 추가 feature (`friends-add` 등) — 닉네임 검색 / 연락처 매칭(ADR-0008) / 친구 요청 흐름 | [friends/spec.md](./features/friends/spec.md) | design + mobile + backend | 1차 2단계 진입 후. 카카오 scope 승인은 외부 의존(🔵). 빈 상태 CTA 라우팅 본 feature에서 연결. |
| 🔴 **전화번호 해시 pepper 도입 — 전화번호를 실제로 쓰는 시점의 선결 조건** | [ADR-0008](./decisions/0008-friend-matching.md) (2026-08-06 deferred) | backend | **지금 당장 할 일이 아니다. 되살릴 때 반드시 먼저 할 일이다.** `users.phone_number`의 **salt 없는 SHA-256은 전화번호에 대해서는 사실상 평문**이다 — 한국 휴대폰 번호 공간이 `010`+8자리 ≈ **1억 개**라 전수 해시 테이블을 노트북으로 몇 분이면 만든다. DB 유출 시 해시가 방어가 안 되고, 복원 가능하면 법적으로도 여전히 개인정보다. → **HMAC-SHA256(서버 pepper)**로 전환(매칭 기능은 그대로, 키는 ADR-0007 환경 분리에 얹음). ⚠️ 대가: **키를 잃으면 기존 해시가 전부 무의미**하고 원본이 없어 재해시 불가 — 로테이션 안 하는 값으로 취급. ✅ **지금 전환 부담 0** — scope 미승인이라 `rawPhoneNumber`가 항상 null이었고 **저장된 row가 0건**이다. |
| Redis 용도 결정 (캐싱? 세션? blacklist?) | repos.json backend.blockers | backend + pm | dependency만 있고 사용처 미정. |
| 라이트 테마 ADR (보류 결정 공식화) | colors.md / 노션 | pm | dark-first 통일됐으나 명문화 안 됨. **ADR-0011**(예정 — 0010은 날짜·시간 모델이 선점). |
| **challenge-create 디자이너 검토 12건** (수락 UI 바텀시트 채택 사후 검토 / 시트 상단 radius 24.dp vs Lovable drawer 기본 10px / 마감 캡션 신규 문구 "선택한 자정까지가 챌린지 기간이야" 외) | [challenge-create/design.md §9](./features/challenge-create/design.md) | design + user | design-bridge가 Lovable에 대응 화면이 없어 기존 토큰만으로 신규 설계한 부분. 신규 토큰 0건. 디자이너 검토는 사후. |
| **Lovable 화면별 문구 톤 불일치** (챌린지 화면 반말 "걸래?"/"받아친다" vs 친구 화면 존댓말) | [challenge-create/design.md §9](./features/challenge-create/design.md) | design + user + pm | **원본 Lovable이 이미 화면별로 갈려 있어** design-bridge가 각 화면 원본 톤을 따랐다. challenge-create 범위 밖 전역 이슈 — 전체 톤 통일 여부는 별도 결정 필요. |
| 디자이너 질의 16건 통합 회신 | 노션 [📋 Common States & PM Questions](https://app.notion.com/p/3523902cbe248111ac2dd40fcd8fda64) | design + user | 16건 묶어 디자이너에게 전달. |
| 🟢 **`EmptyStateCard`(`:core:ui`) CTA 필수 제약 — 2번째 기각 사례** | 2026-08-26 mypage 보관함 → 2026-09-01 [notification-list design §11-②](./features/notification-list/design.md) | design + mobile | `EmptyStateCard` 는 CTA 를 **필수 파라미터**로 받는데, 놓을 CTA 가 없는 화면이 이제 둘이다 — 보관함(빈 달)과 알림 목록(홈이 이미 그 CTA 들을 갖고 있고 사용자는 방금 거기서 왔다). 둘 다 전용 빈 상태를 따로 만들었다. 🔴 **지금 통합하지 않는다** — 2건은 우연일 수 있고, 통합 시점에 필요한 건 "CTA 를 optional 로 여느냐"가 아니라 **세 사례의 공통 형태**다. 3번째 사례가 나오면 그때 판단한다. ⚠️ 정리 묶음에 함께. |
| 🟢 **홈 벨 뱃지 위치 육안 검증** (실기 1회) | 2026-09-01 [notification-list design §11-①'](./features/notification-list/design.md) | design | 정본은 6px 오프셋, 구현 `HomeTopBar.kt` 는 10dp. **40dp 버튼에 24dp 아이콘이라 남는 여백이 서로 다르므로 숫자를 맞추는 게 정답이 아니다.** notification-list 로 **이 점이 처음 켜지므로**(그전엔 `false` 하드코딩이라 한 번도 표시된 적이 없다) 실물을 보고 판단한다. 🔴 지금은 고치지 않는다 — 켜진 걸 본 적 없이 좌표만 옮기면 근거 없는 변경이다. |
| 🟢 **enum 밖 `type` 이 `CHALLENGE_REQUEST` 로 오분류된다** (실측 재현됨, 오늘 도달 경로 없음) | 2026-09-01 [notification-list backend-report 미해결 1](./features/notification-list/backend-report.md) · [api-contract §5](./features/notification-list/api-contract.md) | backend | `NotificationEntity.toDomain()` 이 모르는 `type` 을 `CHALLENGE_REQUEST` 로 떨어뜨린다. 🔴 **나쁜 종류의 fallback** — `CHALLENGE_REQUEST` 는 **실재하고 탭되는** 타입이라 모르는 알림이 *"챌린지 신청"* 으로 보이고 탭하면 상세로 간다. 앱 폴백(모르는 타입 → 비탭)은 **모르는 값이 앱까지 가야 작동하는데 서버가 그 전에 아는 값으로 바꾼다.** 🔵 **오늘 도달 경로 없음** — `fromDomain` 이 `type.name` 만 쓰므로 DB 값은 enum 8종뿐이고 8종 전부 round-trip 확인. **트리거: (1) 다중 인스턴스 롤링 배포(ADR-0007) (2) enum 밖 타입 유입.** 🔴 **pm 지시 정정 기록**: 내가 *"목록은 `toDomain()` 을 우회해 컬럼에서 바로 프로젝션하라"* 고 냈는데 **실행 불가능한 지시였다** — repository 인터페이스가 도메인 타입을 반환하고 `Notification.type` 이 enum 이라 거기가 병목이다. 계층 우회 읽기 경로는 이 레포 DIP 규약 위반. 엔티티에 값이 있는 것과 **그 값이 응답까지 갈 길이 있는 것**은 다른 문제다. 고치려면 도메인 모델을 String 으로 바꾸거나 `UNKNOWN` 추가 — 발송부·문구·테스트로 번진다. **관측 전엔 안 고친다**(프로젝트 방침). |
| 🟢 **알림 목록용 인덱스 `(user_id, id DESC)` 미생성** | 2026-09-01 [notification-list backend-report 미해결 3](./features/notification-list/backend-report.md) | backend | 커서 조회가 `WHERE user_id = ? AND id < ? ORDER BY id DESC` 인데 전용 인덱스가 없다(V1 의 `idx_notifications_user_read_created` 는 읽음/시각 축). **관측 후 추가가 방침** — 1줄 마이그레이션이고 계약 영향 0. 실사용 알림 수가 커질 때. |
| 🟢 **`PlaceholderScreen`(`:core:ui`) 소비처 0건 — 컴포넌트만 남았다** | 2026-09-01 notification-list T-M1 (mobile 실측) | mobile | `MainScreen.kt:228` 이 `NotificationRoute()` 로 바뀌면서 **앱 전체에서 `PlaceholderScreen` 을 쓰는 곳이 사라졌다** — 알림이 마지막 placeholder 였다. 🔵 **이건 부채가 아니라 이정표다**: 기획서 §4 IA 가 정의한 진입 경로 중 **빈 화면으로 끝나는 것이 하나도 안 남았다.** 컴포넌트는 `:core:ui` 에 존치(삭제는 그 feature 범위 밖이라 mobile 이 남긴 판단이 맞다). ⚠️ **지금 지우지 마라** — 다음 신규 화면이 골격을 먼저 올릴 때 쓰인다. 정리 묶음에서 *"한동안 소비처가 다시 안 생겼는지"* 를 보고 판단할 대상이지, 0건이 곧 삭제 사유는 아니다. |
| 🟢 **알림 목록 실기 검증 0건** — 무한 스크롤 트리거 · 뱃지 점등/소거 | 2026-09-01 [notification-list/summary.md](./features/notification-list/summary.md) | user + mobile | 단위·시뮬레이터까지만 확인(Android 162 · iOS 162). 🔴 **실기로만 갈리는 것 둘**: ① 마지막 항목 도달 시 다음 쪽 로딩이 실제로 걸리는지 ② **목록에서 뒤로 나온 홈에서 점이 사라지는지**(design §4.5.1 — 남으면 *"읽었는데 왜 아직 있지"* 가 되어 **뱃지를 켠 것이 오히려 신뢰를 깎는다**). ②는 `WhileSubscribed(0)` 자동 재조회에 기댄 결론이라 구조가 바뀌면 조용히 깨진다. |
| 🟢 **알림 목록 기준 시각 공유가 간접 검증** | 2026-09-01 [notification-list/mobile-report.md](./features/notification-list/mobile-report.md) | mobile | 경과 시각은 `now` 를 UiState 가 1회 받아 전 항목이 공유해야 한다(항목마다 `nowKst()` 를 부르면 같은 목록 안에서 기준이 어긋난다). 그 공유가 **직접 검증되지 않고 간접으로만** 확인됐다. mobile 이 숫자 옆에 묻지 않고 별도 절로 뺀 항목. |

## 🔵 대기 (외부 의존, 사용자 액션)

| 항목 | 사유 | 후속 영향 |
|---|---|---|
| 🎨 **디자이너 확인 6건 — 재촬영 UI + Done 문구** | Lovable 에 **인증/카메라 화면이 0건**이고 "재촬영" 상태도 없어 mobile-dev 가 기존 토큰·패턴으로 구현했다. design-bridge 를 팀에서 제외한 이유이기도 하다 | 문구 `다시 찍기` / `IconTextButtonStyle.Outlined`(주 액션이 아니라서 — Filled 면 완료 표시보다 강조돼 *"아직 인증 안 됐나"* 로 읽힌다) / `PhotoCamera` 아이콘 / 사진 아래 12dp / `bold16` / 🔴 **완료 뱃지를 그대로 둔 채 재촬영 버튼을 얹는 조합에 이 앱 선례가 없다** / Done 문구 🟡 초안 `사진이 교체됐어요. 상대는 최신 사진을 보게 돼요`. Preview: `ChallengeDetailScreenVerifiedWithRetakePreview`, `MissionCardMineWithPhotoAndRetakePreview` |
| ~~Kakao `account_phone_number` scope 승인~~ | ~~Kakao 개발자 콘솔 신청·승인~~ | ✅ **2026-08-06 대기 해제(폐기)** — 기획서 §2.2에서 연락처 매칭이 §6으로 이동. **외부 의존 대기 항목이 하나 줄었다.** 되살리려면 위 🟢 pepper 항목이 선결. |
| 🔴 **`KAKAO_ADMIN_KEY` 발급** (카카오 콘솔 — 사용자 직접) | 사용자 콘솔 작업 | **회원탈퇴의 카카오 unlink 가 이 키로 동작** (admin key 방식 — 서버가 카카오 토큰을 저장하지 않아 토큰 방식은 카카오 세션 만료 시 탈퇴 자체가 불가). 미설정이면 unlink 건너뛰고 DB 익명화만 수행(WARN — FCM 키 선례와 같은 fail-soft). 🔴 **운영 배포 체크리스트 항목.** |
| 🔴 **Apple Developer Account ($99/년) 구입** | iOS 빌드/배포 **+ APNs 인증키 발급** | iOS TestFlight 배포 시점 + 🔴 **iOS 푸시 수신 전체.** ⛔ **`push-fcm`의 iOS 수신이 여기에 막혀 있다 — 기술 선택이 아니라 경로 부재다.** APNs 인증키(`.p8`)와 Push Notifications capability가 유료 계정 전용이고, **시뮬레이터로도 우회 불가**(APNs 토큰을 못 받아 FCM 토큰 자체가 안 나온다). `soul-oath` iOS 실기 미검증(기기 부재)과 성격이 다르다 — **결제 전에는 부분 검증조차 불가능**하다. ADR-0005("iOS+Android 동시 MVP")가 처음 깨지는 지점이라 **결정 기록 필요**. 현재 iOS actual은 전부 no-op 스텁이고 `iosApp/` 무접촉, `.entitlements` 0건(정상 상태 — 무료 계정으로 켜면 실기 빌드가 깨진다). |
| Lovable 디자인 추가 화면 export (**영혼의 맹세** + **인증 촬영 화면** — 2026-08-25 기준 둘) | 디자이너 작업 | 촬영 화면은 challenge-verification 이 디자인 없이 구현(시스템 카메라 + 미리보기, 교체 전제). ~~전화번호 등록~~은 기획서 개정으로 폐기. |
| **챌린지 상세 디자이너 확인 7건** ([challenge-verification/design.md §7](./features/challenge-verification/design.md)) | 디자이너 확인 | 내 인증 후 카드 상태(미러링)·판정 대기 표기·마감 행 존치(유일한 Lovable 이탈) 등 — **전부 보수적 제안값으로 구현 완료**라 차단은 없고, 디자이너가 뒤집으면 교체. §7 표가 "우리가 정한 값 vs 승인값" 구분 대장. |
| 🔵 **EXIF 서버측 미가공 — 실사용자 유입 전 재검토** | 실사용자 유입 시점 | 서버는 받은 바이트를 무가공 저장·서빙. 앱은 `JpegMetadata.strip` 으로 제거하지만 **앱 아닌 클라이언트가 EXIF 를 실으면 그대로 저장**된다. 유입 전 재검토 조건부 부채([backend-report 미해결](./features/challenge-verification/backend-report.md)). 뒤집으면 저장 직전 재인코딩 1지점. |
| ~~**Firebase 프로젝트(dev) 생성**~~ ✅ **2026-08-07 완료** — `challenge-52606`. Android 앱 등록 + `google-services.json` + 서비스 계정 키 발급까지. **prod는 배포 시점으로 이월**(ADR-0007이 local 단계) | ~~사용자 액션~~ | 아래는 당시 기록. iOS 제약은 **여전히 유효**하다 |
| ~~(구)Firebase 프로젝트(dev + prod) 생성~~ | 사용자 액션 — 콘솔 작업 | 🔴 **`push-fcm` 착수의 유일한 관문.** Android는 `google-services.json`만 받으면 열린다. ⛔ **iOS는 APNs 인증키(.p8)가 필요한데 그 키는 Apple Developer Account($99/년)에서만 발급된다** — 즉 iOS 푸시는 계정 구입 전까지 원천 불가이고, **Android 먼저 / iOS 나중**의 phased가 선택이 아니라 제약으로 강제된다. ADR-0005("iOS+Android 동시 MVP")와 명시적으로 어긋나는 지점이라 결정 기록 필요. |
| ~~**서버 `WireShapeContractTest.kt` 커밋**~~ ✅ **2026-09-05 해소** — `51f1117` 에 실려 커밋됐다(`git ls-files` 실측). 아래는 당시 기록 | ~~사용자 액션 (에이전트 git 금지)~~ | #25 감사의 **유일한 산출물**. `scripts/e2e-off.sh`가 한 번 유실됐던 것과 같은 구조 — 커밋 전까지는 없는 것과 같다. ✅ **2026-08-06 실측: 미커밋은 이것 1건(untracked)뿐이다.** 위 "직전 갱신"과 여러 "최근 완료" 항목에 적힌 **"3·4개 레포 커밋 0건"은 stale** — 모바일은 working tree clean(`101a4f9`까지 커밋됨), 서버도 soul-oath 커밋됨(`e5eade0`). 과거 기록은 그대로 두되 현재 사실은 이 줄이 정본이다. |
| 🔵 **알림 화면 정본이 4개월 반 낡았다 — 디자이너 확인 2건** ([notification-list design §10-①·⑤](./features/notification-list/design.md)) | 디자이너 확인 | `notifications.tsx` 는 `634e170` **2026-04-16** 이후 무수정인데(레포 HEAD 는 2026-09-01 — pm 실측 확인), 그 뒤 타입 체계가 **두 번** 바뀌었다(2026-08-06 `SIGN_REQUEST` 폐기 + `ACCEPTED`/`REJECTED` 신설, 2026-08-14 `OPPONENT_VERIFIED` 발송 개시). 🔴 **그래서 정본 mock 6종과 발송 4종은 "일부 미구현" 관계가 아니라 서로 다른 목록이다** — 이 구분을 놓치면 없는 타입을 구현하려 든다. ① 알림 화면 재작업 계획이 있는지 ② 발송 4종 중 `CHALLENGE_ACCEPTED`·`CHALLENGE_REJECTED` 는 **정본에 아예 없어 아이콘·색을 앱 관례에서 유도**했으니 승인 필요. **차단 아님** — 낡은 것은 타입 목록이지 카드 형태가 아니고 형태는 다른 화면과 정합해 재-export 없이 진행했다. 뒤집히면 아이콘·색 교체 1지점. |
| 🟡 **알림 문구 조사 결함 — `"민수과 계약을 하시렵니까?"`** ([notification-list backend-report 미해결 2](./features/notification-list/backend-report.md) · design §3.3.1) | **사용자 결정** (2026-08-07 사용자 확정 문구라 임의로 안 바꿨다) | `NotificationMessages` 의 `"{actorNickname}과 계약을 하시렵니까?"` 가 **받침 없는 닉네임에서 `"민수과"`** 가 된다. 4종 중 이 한 종만 `님` 도 빠져 있다(나머지는 `{닉}님이`). 🔴 **확정 당시엔 푸시밖에 없었다는 것이 핵심** — 푸시는 스쳐 지나가지만 **목록은 남고 스크롤하면 다시 보인다.** 노출 조건이 달라졌으므로 재확인 대상이지, 결정을 뒤집자는 게 아니다. **고칠 자리는 서버 `NotificationMessages` 한 곳, 앱 변경 불필요.** 그대로 두는 것도 유효한 선택. |

## ADR pending / in-progress

| ADR | 상태 | 비고 |
|---|---|---|
| [ADR-0003 모바일 템플릿 초기화](./decisions/0003-mobile-template-init.md) | in-progress (5/6 완료, `:feature:home` 교체 ✅ home-feed 2026-05-25) | 잔여 1건: `challenge-app/CLAUDE.md` 모듈 구조 정정 (위 🟢 항목 참조). 정리 후 accepted 전환. |
| [ADR-0006 STT 제공 방식](./decisions/0006-stt-strategy.md) | **superseded (2026-08-06)** | 기능 자체가 기획서 §2.4에서 삭제되어 A/B/C 선택이 무의미. 딸려 폐기: 노션 Server 스펙 3.11 `POST /api/v1/stt/convert`. 되살아날 조건은 §6 "음성 맹세" 채택 — 그땐 이 문서가 아니라 **파일 스토리지 ADR**이 쟁점이라 새 ADR을 쓴다. |
| [ADR-0008 전화번호 등록 플로우](./decisions/0008-friend-matching.md) | **deferred (2026-08-06)** | A안 결정 자체는 유효. 소비할 기능(연락처 매칭)이 §6으로 이동해 착수만 미룸. **컬럼·`PhoneHasher`·해시 저장 배선 전부 유지** — scope 승인 하나로 되살아나는 상태로 동결. 되살릴 때 pepper 도입이 선결. |
| [ADR-0009 Refresh Token Rotation](./decisions/0009-refresh-token-rotation.md) | **accepted (2026-05-28)** | DB sha256 hash 기반 rotation 채택. ADR-0002 401 의미 세분화 하위 결정 포함. [auth-refresh-rotation](./features/auth-refresh-rotation/summary.md)로 구현 완료. |
| [ADR-0010 날짜·시간 모델 `LocalDateTime` 통일](./decisions/0010-datetime-model-localdatetime.md) | **accepted (2026-07-31) — 구현 대기** | 사용자 결정. 선결 7건 ADR에 정리. 별도 feature로 분리 예정. |
| [ADR-0011 인증 사진 저장](./decisions/0011-photo-storage.md) | **accepted (2026-08-14) — §3·§4 수단 superseded (2026-08-18)** | 로컬 폴더 + `PhotoStorage` 포트는 유지. §3 URL 발급 → multipart 직접 수신, §4 서명 URL → JWT 서빙 — 둘 다 사용자 결정("안 쓰는 유연성에 복잡도를 지불하지 않는다"). [challenge-verification](./features/challenge-verification/summary.md)으로 구현 완료. |
| [ADR-0012 목록 페이지네이션 — 커서(`id` 기준)](./decisions/0012-list-pagination-cursor.md) | **accepted (2026-09-01)** | pm-lead 결정 · backend-dev 제안·구현·검증. notification-list 최초 적용. 규칙 줄(CLAUDE.md)은 결론을, ADR 은 근거 3건(offset 기각 / `created_at` 배제 / 불리언 금지)과 **차등 검증 기법**을 보관한다. 기존 6종 미적용은 **미완성이 아니라 결정**. |
| 라이트 테마 ADR (보류 결정) — **번호 미배정** | 미작성 | 위 🟢 "라이트 테마 ADR" 트리거. 🔴 **번호를 미리 예약하지 마라. 이번이 두 번째 충돌이다** — 0011 을 인증 사진 저장이, 0012 를 페이지네이션이 선점했다. 원인은 구조적이다: **예약은 이 백로그에만 있고, ADR 을 쓰는 사람은 `docs/decisions/` 디렉터리만 본다**(backend-dev 도 "0011 이 마지막"을 확인하고 0012 를 썼다 — 맞는 절차다). **쓰는 시점에 그때의 다음 번호를 붙인다.** |

## 분류별 보기 (담당자 기준)

### 사용자 액션 (user)
- 🟢 **`verification-photo-replace` T-I1 ④ 서버 실파일 삭제 육안** — ✅ 2026-09-05 ①②③ 통과로 강등된 잔여 1건. 비블로커(테스트 4건이 삭제 key·순서까지 단언 + `delete` 는 회원탈퇴가 쓰던 기존 구현)
- 🔴 **컨테이너 런타임 설치** (Docker Desktop / OrbStack / Colima 아무거나) → 통합 테스트 45건 일괄 해소
- ~~**4개 레포 커밋 결정**~~ ✅ **2026-09-05 해소 — 4개 레포 전부 uncommitted 0건**(hub `9a77bb4` / server `e2f57f5` / app `2a650ba` / Lovable `b07634c`). 🔵 **남은 건 푸시다** — server 14 · app 10 · hub 미푸시분, Lovable 만 0
- **challenge-create 실기기 시각 검증** (위저드 / 받은 도전장 / 수락 다이얼로그 iOS IME + 로컬 네트워크 권한 "허용")
- 🔴 **Firebase 프로젝트 생성** (`push-fcm` 착수 관문) / Apple Developer(= iOS APNs 키) — ~~Kakao scope 승인~~은 2026-08-06 폐기
- 디자이너에게 **영혼의 맹세** 화면 + 16건 질의 전달 (~~전화번호 등록 화면~~ 폐기)
- Refresh rotation E2E smoke (access 짧게 → 무자각 갱신 / refresh 만료 → 로그인 자동 이동 / V1 가입자 강제 재로그인 — auth-refresh-rotation 후속)

### 모바일 (mobile)
- 🔴 **`:remote:datasource` iOS 테스트 미컴파일** — 백틱 이름의 `,()` (Kotlin/Native 금지) 15건. **그 모듈 iOS 커버리지 0**
- 🟡 **`MissionCardMineFailedPreview` 정정/삭제** — `FAILED` → 슬롯 `NONE` 인데 Preview 는 재시도 버튼을 그린다 (`ai-verification` 에서 함께)
- 위저드 시스템 백 버튼 (`PlatformBackHandler` → `:core:ui` 승격)
- challenge-create iOS 유닛 테스트 (`:feature:home` / `:feature:challenge:create`)
- `SearchProfilePlaceholder` 4번째 사본 흡수
- iOS Keychain 실기기 smoke
- iOS SPM 카카오 SDK 버전 pin 적정성 확인
- detekt config 부재 정리 (home-feed에서도 동일 영향 확인)
- Compose blur 미적용 (LoginScreen + BottomBar 동일 카테고리, `BlurEffect` 도입 검토)
- Material Icons Extended deprecation → Material Symbols 마이그레이션 ADR 후보 (`:feature:home` 가세로 사용처 누적)
- Glow shadow / 외광 효과 (Canvas glow)
- 카카오 버튼 ripple 색 매핑
- `:feature:login` `@Preview` deprecation 경고 정리 (즉시 가능)
- LoginScreen 10sp 라벨 `bold10`/`light10` 슬롯 정합 (tokens.md §5.2 후속, 즉시 가능)
- FAB `pulse-fire` 애니메이션 미구현 (HomeScreen — glow 보류 확정, 모션은 alpha 호흡으로. 2026-08-26 문구 정정)
- EmptyState 잔여 통합 — 🔴 **사본이 2개가 아니라 3개였고 이미 값이 갈렸다** (2026-08-26 실측, loser-ranking design.md §4.4.1): `FriendsEmptyState` / `HomeEmptyState` / `FriendPickPlaceholderCard`(KDoc 이 "internal 이라 재사용 불가로 별도"라 자백). 같은 64dp 일러스트 박스의 모서리가 **16dp / 16dp / Circle 로 셋 다 다르고 Lovable 정본(20dp)과도 전부 다르다.** loser-ranking 이 `FriendsEmptyState` 를 `:core:ui` `EmptyStateCard` 로 승격(icon·ctaIcon·보조 CTA 전부 파라미터화, 렌더 무변경 — 4번째 사본 방지). **잔여 = 홈 + FriendPickPlaceholderCard 2건 이관 + 정본 20dp 정합.** 지금 정본에 맞추려면 3곳, 통합 후엔 1곳이다 — 이게 통합의 근거. "한 곳이 바뀌면 나머지가 조용히 갈린다"(flowUtil 항목 논거)의 실증 사례
- ChallengeCard 시간 표시 자동 갱신(분 단위)
- Ktor `Auth(bearer)` 단위 테스트 (auth-refresh-rotation 후속)
- 백틱 테스트명 Kotlin/Native 위반 rename → 3개 모듈 iOS 테스트 개통 (**iOS 커버리지 +89건**, 로직 무변경. `:local:database` ksp 실패는 별건)
- `IconTextButton` 높이 52dp 하드 고정 — 폰트 스케일 미추종, 2.0x 에서 `bold16` lineHeight 클리핑 직전 (2026-08-25 T-M4 중 발견, 접근성 항목. challenge-verification/mobile-report.md 21번 참조)
- 탭 헤더 타이포 정합 — Lovable 4탭 헤더는 전부 20px 인데 `ChallengeTopBar` 기본이 `bold18` 이라 **친구 탭만 어긋난 채 출시**(홈·랭킹·마이페이지는 `bold20` 지정). 🔴 기본값 교체는 하위 화면(상세·생성·검색·보관함) 상단바가 전부 커지므로 불가 — 탭/하위 구분 설계 필요 (2026-08-26 mypage design 실측)
- `ConfirmDialog` ↔ `PermissionSettingsDialog` 통합 — 버튼 색 배치가 서로 반대라 이번엔 신설로 감. 3번째 사용처 발생 시 통합 판단 (mypage design)
- 통계 화면 feature 예고 — 월별 캘린더(성공/실패)+추이. 착수 시 마이페이지 메뉴 **첫 행**으로 복귀(정본 순서), 일자별 결과 조회 API 신규 필요, tokens.md §8-5 차트 5색 재개 후보(단 2값이라 기존 success/error 로 충분할 공산 — "다색 디자인이 먼저" 조건 유지)
- T-M4 잔여 검증 3건 (Coil 404 무캐시 런타임 / 티커 갱신 / CTA 첫 화면 이탈)
- iOS 촬영 실동작 실기기 검증 (시뮬레이터 카메라 부재)
- `profileImageUrl` 화면 적용 (로더는 도입됨 — challenge-verification T-M4)
- flowUtil 정리 — `flow { }` 반복 7파일 10곳 (2026-08-03 사용자 지시, 미착수)

### 백엔드 (backend)
- 🟡 잘못된 요청 본문 → HTTP 500 (`HttpMessageNotReadableException` 핸들러) — `datetime-model-migration` T-B3에 포함 확정
- `/actuator/health` 500 (actuator 의존성 부재)
- ~~`EXPIRED` 전이 스케줄러 (`:batch`)~~ ✅ 2026-08-25 해소 (challenge-result 배치)
- 동시성 보호
- `AuthServiceRefreshTest` 단위 테스트 5 케이스 (auth-refresh-rotation 후속)
- logout 엔드포인트 구현 (refresh hash NULL화 + Spring Security 인증)
- Redis 용도 결정
- HomeIntegrationTest 3건 수동 verify (Docker)
- ~~판정(결과) feature~~ ✅ 2026-08-25 완료 (challenge-result)
- RESULT·REMIND 푸시 추후 도입 (배관 재사용 + §0.6.1 절차 — 사용자가 원할 때)
- 배치 다중 인스턴스 DB 락 + cron 실발화 검증 (ADR-0007 배포 시점)
- 인증 사진 잔여 4건 (보관 정책 / `photo_url` 개명 / multipart 상한 테스트 / 고아 파일)
- 🟡 `storage-root` 배포 시 절대 경로 (`CHALLENGE_PHOTO_ROOT`, ADR-0007)
- 🔵 EXIF 서버측 미가공 — 실사용자 유입 전 재검토
- ~~`verifications` PENDING row 자동 생성 (챌린지 IN_PROGRESS 진입 시)~~ — ✅ **2026-08-14 실측 정정: 이미 구현돼 있다.** `ChallengeCommandService.accept()` 가 수락 트랜잭션 안에서 `pendingVerification()` 2건(challenger·opponent)을 `saveAllAndFlush` 한다(`soul-oath` 때 들어감). 105행에 같은 항목이 이미 해소로 적혀 있는데 **여기 사본이 남아 미구현으로 보였다** — 미구현으로 두면 **인증 feature 스코프가 부풀어 보인다**(backend-dev 발견)
- ~~user_stats 자동 집계 트리거 (결과 확정 시)~~ ✅ 2026-08-25 해소 (challenge-result)
- 챌린지 deadline UTC 저장 정책 명문화 (생성 feature 진입 시)

### 디자인 (design)
- 🎨 **재촬영 UI 6건** (verification-photo-replace) — `다시 찍기` 문구 / `Outlined` 스타일 / `PhotoCamera` 아이콘 / 사진 아래 12dp / `bold16` / 🔴 **완료 뱃지를 둔 채 재촬영 버튼을 얹는 조합에 앱 선례 없음** + Done 문구 🟡 초안
- Lovable 레포 lint 오염 37 errors — 미접촉 13개 파일(login·notifications·friends·ChallengeCard·ui/* 등)의 기존 prettier 위반. 새 작업 diff 가 번지지 않게 별건 일괄 정리 (2026-08-26 archive 프리뷰 중 실측)
- 영혼의 맹세 화면 + **인증 촬영 화면** (디자이너 대기 화면 2개 — 촬영은 구현 완료·교체 전제)
- 챌린지 상세 디자이너 확인 7건 (challenge-verification/design.md §7 — 차단 없음, 승인 대장)
- 결과 UI 확인 — 홈 "최근 결과" 구획 제목(시안에 없는 신규 문구) + 결과 pill + 기획 §2.6 "결과 카드" (challenge-result)
- 알림 문구 4종 확정 (초안 3 + 확정 1, user 와 공동)
- ~~전화번호 등록 화면~~ — 2026-08-06 기획서 개정으로 폐기
- 시각 검증 5건 회신 (+ home-feed 시각 검증 1건)
- 16건 통합 회신
- home-feed 결정 4건 회신 (FAB / CTA 수 / 일러스트 / Swords 아이콘)

### PM (pm)
- ADR-0009 / ADR-0010 작성 결정
- 디자이너 질의 통합 전달

---

## ✅ 최근 완료 (최근 10건만 유지, 초과 시 archive로)

- **2026-09-05 — feature `verification-photo-replace` 완료** (`completed`. **인증 사진을 마감 전까지 몇 번이든 다시 찍어 바꿀 수 있다.** 재제출 **전면 거부(`code=700`) → `last-write-wins`**. 🔑 **마이그레이션 0 / 새 엔드포인트 0 / 요청·응답 shape 변경 0** — 바뀐 건 *같은 요청에 대한 서버의 행동*뿐이라 기존 계약 2건을 **개정**했다(신규 계약 없음). 🔑 **실제 난점은 재제출이 아니라 캐시였다** — 사진 경로가 교체돼도 그대로라(`/photos/{party}`) 서버·앱 양쪽이 옛 사진을 캐시하고 있었고, 그대로 뒀으면 *"다시 찍어도 화면이 안 바뀌는데 에러도 로그도 없는"* 상태로 배포됐다(테스트는 전부 초록). **두 팀원이 각자 계층에서 독립 발견** → 서버 `no-cache`+ETag/304 / 앱 Coil 캐시 키 `photoUrl+verifiedAt`. **서버 574/574**(50 skipped=Docker, 신규 21 · 🔴 *거부* 테스트 2건 삭제 — 남았으면 이제 틀린 것을 지키는 테스트가 된다) + throwaway DB 실기동 / **모바일 Android 112 · iOS 73 failures 0**(pm 이 XML 직접 집계). 🔴 **삭제가 커밋 뒤임을 구조로 강제** — 트랜잭션 경계를 별도 빈으로 분리해, 바깥 `submit()` 에 `@Transactional` 을 되붙이면 깨지도록 만들었다. ✅ **T-I1 실기 3/4** (2026-09-05 사용자 육안): 내 교체 왕복 · 🔴 상대 교체 → 내 화면 · 알림 1회만 — **단위 테스트가 원리적으로 못 잡던 항목이 정확히 앞의 둘**이다. **부수 발견**: 🔴 `:remote:datasource` iOS 테스트가 **한 번도 컴파일된 적 없다**(백틱 이름의 `,()` — Kotlin/Native 금지, 15건 / 증거는 `build/test-results/` 에 iOS 디렉터리 자체가 없음 = 그 모듈 iOS 커버리지 0) → 백로그 🔴 존치. 🔴 **T-M4 를 조용히 되돌릴 계약 문장 1건 삭제**(남았으면 다음 사람이 캐시 키에서 `verifiedAt` 을 뺐다). 🔴 **pm 자기 정정 3건** — 해제 기준 자기모순(교착 직전) / **승인을 실측표에 ✅ 완료로 기록**(grep 0건으로 적발, 조용히 닫혔으면 아무도 구현 안 함) / 남의 관측을 확인 없이 내 질문으로 전달. ⚠️ 팀 운영 사고 1건 — 이전 세션 **잔여 팀원**에게 통지 오발신(무해 종료 → `run-feature` 에 spawn 전 잔여 확인 단계 신설). **미해결**: 🟢 T-I1 ④ 서버 실파일 삭제 육안(비블로커) / 🔴 `:remote:datasource` iOS 미컴파일 / 🟡 `MissionCardMineFailedPreview` 낡음 / 🎨 재촬영 UI 디자이너 확인 6건 / 🔵 양 레포 미푸시.)
- **2026-08-26 — feature `mypage` 완료** (`completed`. **4탭 완성 + 계정 수명주기 마감** — 프로필 카드(전적 실데이터) / **계약서 보관함**(`GET /challenges/history`, 월별 카드 — 기획 §3.3 이행이자 백로그 🟡 "결과 히스토리 부재" 정식 해소) / **로그아웃 실구현**(백로그 🟡 해소 — 구 술어에서는 401 자동 갱신조차 안 돌던 결함 발견·수정) / **회원탈퇴**(`DELETE /users/me` — 익명화+기록 보존+사진 삭제, unlink→DB→파일 순, PENDING·notifications 삭제 확정, 타 응답 계약 변경 0). **백엔드 513 중 464/49skip**(신규 47, 회귀 0) + 🔴 **throwaway DB 실구동 2회**(이 레포 단위 테스트는 JPA mock 이라 JPQL·V11 이 어디에도 안 걸림 — `PhotoStorage.delete` 첫 실동작, 재로그인=신규 계정, 탈퇴 후 상대 화면 무파손 실측). **모바일 A346/iOS259**(로그아웃 특성화 10건 — `WWW-Authenticate` 없이 갱신됨을 실증해 **서버 전역 401 변경 차단**). Lovable 프리뷰 `/archive` 신규+`/mypage` 갱신(`c578d00`). **주요 결정**: photoDeleted additive(pm 의 조합 재정의 지시를 팀이 뒤집음 — 감지기 소거+조용한 오독 논거) / 승률·패율 분모 통일(CLAUDE.md 전역 승격, 두 탭 합 112% 모순 차단) / 실패 표시 3원칙(가짜 0 금지·전체 실패=인라인 카드·부분 실패=스낵바 병행). **design 자기 오류 6건 전부 구현 전 적발**(경로 3종: 교차 검증·회귀 케이스·프리뷰) + 문서 교훈 3건(기대값에 근거·`.5` 케이스·금지 지시의 예외는 같은 자리). **미해결**: 디바이스 실기 0(서버 재기동+V11 필요) / `KAKAO_ADMIN_KEY`(사용자) / ⑭ 조회 단위 재확인 / 문구 미확정.)
- **2026-08-26 — feature `loser-ranking` 완료** (`completed`. **개돼지 랭킹 — 4탭 placeholder 0 달성.** 2026-08-06 기획 축 반전(명예의 전당→패자 랭킹)의 구현, challenge-result 집계의 첫 소비자. **백엔드**: `GET /rankings/losers` 신규 1건 — 정렬 4키(`losses→lossStreak→lossRate→userId`, 패율 1차면 "1전 1패가 개돼지왕" 서사 역전 방지, userId 는 0전 동률의 결정성 보장), 공동 순위 없음(포디움 방어를 서버가 원천 차단), `isMe` 서버 부여(amIChallenger 기각과 갈린 근거 = 실패 모드 방향), `totalChallenges: Int`(0전 "아직 기록 없음" — Int 는 사실을, Boolean 은 규칙을 소유), rankings 최소 1건 불변식(빈 상태 = size==1). **417/417** + 실서버 실측(LEFT JOIN 폴백은 계정을 심어 검증 — native query 라 슬라이스가 원리적으로 못 덮는 유일 축). **모바일**: A**460/460**·iOS**148/148**(신규 44) — 계약 9필드 필드별 대조 차이 0, 실서버 원문 픽스처(재정렬하면 반드시 깨지는 단언), EmptyStateCard 승격(3번째 사본 방지), 백틱 3모듈 회피 배치(의도 명시). **디자인**: ranking.tsx 정본 채택(이미 개돼지로 완성돼 있었음), 애니메이션 판정선 "transform·alpha 구현/glow 보류"(기존 선례 3개), 정본 이탈 5건 사유 기록, 정본의 죽은 CSS(`fire-gradient/20`)까지 실측. **전역 발견**: ADR-0002 인증 실패 표기 오류(실제 bare 401) 정정 + 일괄 감사 등재 / 카카오 URL http 평문 → 아바타 활성화 시 iOS 전용 ATS 차단(https 정규화 + fname 함정 등재). **팀 자기 교정 3건**(기각 사유 과장 / 실측→스코프 월권 / 문서 사본 — 전부 에이전트가 스스로 잡아 철회·박제). **미해결**: 디바이스 실기 / 디자이너 확인 12건 / 친구 0명 불변식 실측 미커버.)
- **2026-08-25 — feature `challenge-result` 완료** (`completed`. **핵심 플로우 마지막 구간 — 이제 챌린지가 스스로 끝난다.** 자정(00:05 KST) 배치가 §2.6 판정(`COMPLETED`+result 4종) + 미인증 `FAILED` + 방치 `PENDING`→`EXPIRED` + **전적 집계를 단일 트랜잭션**으로. 이 프로젝트 **첫 스케줄러**(`:batch` 첫 코드). **백엔드 392/392**(신규 72) + **모바일 Android 186/186·iOS 165/165**(신규 68, 마감 후 active 원문 픽스처 9건 추가분 포함). 🔴 **실서버 실측 2회** — 마감 최대 21일 경과 실데이터로 소급 판정 증명(judged=6/expired=2, notifications 14→14 = 푸시 제외 검증, 재실행 멱등 해시 동일), V10 연패 2컬럼 실DB 적용. **홈 전적이 처음으로 실데이터**가 됐고 개돼지 랭킹 데이터 준비 완료. **주요 결정**: RESULT·REMIND 푸시 제외(사용자 — 미발송을 퍼블리셔 미주입으로 구조 보장) / 홈 COMPLETED 7일 노출(히스토리 부재로 3→7 상향) / `myResult` 4값(BOTH_LOSE 안 접음 — 접으면 판정 규칙 사본을 앱에 강제) / **소급은 스케줄러가 아니라 쿼리가 보장(보상 로직 금지)** / 연승은 home-feed confirmed 계약의 **이행**(협의 쟁점이 아니었음을 발견 — 집계 규칙이 읽는 쪽 계약에 살고 있었다) / 연패는 그 거울상. **신규 엔드포인트 0** — confirmed 계약 2개 확장 + 양쪽 change-log 등재. **해소한 백로그**: EXPIRED 전이 스케줄러 / user_stats 집계 트리거 / 연패 컬럼(V10) / 판정 FCM 제외 결정(이행). **미해결**: 디바이스 실기 확인 / "최근 결과" 문구 등 결과 UI 디자이너 확인 / 승·무의 연패 끊기 분기 실측 미관측 / 결과 히스토리 화면(7일은 시간벌기).)
- **2026-08-25 — feature `challenge-verification` 완료** (`completed`(Android). **핵심 플로우의 "양측 인증(카메라)" 구간 개통** — 촬영→압축→제출→상대 사진 확인 실기 왕복. **백엔드**(`1ae7dc8`): multipart 제출 + `PhotoStorage` 포트/로컬 디스크 + JWT 사진 서빙 3종 엔드포인트, `OPPONENT_VERIFIED` 발송 개시(§0.6.1 절차 준수), V9 주석만(DDL 0건). **320/320**. **모바일**(`74e2f74`+`ec6f9d4`): `:core:camera`(시스템 카메라 expect/actual, iOS 실구현) + `:feature:challenge:verify`(촬영→미리보기→확정) + **상세 재구성**(2026-08-24 사용자 기획 3건 → Lovable 정본: VS 헤더+남은시간 티커/미션 카드 2장/압축 계약서+마감 행 존치) + **Coil 3 첫 배선**(기본 인증 클라이언트) + `JpegMetadata.strip`(순수 Kotlin EXIF 제거, commonTest 고정). detail **29/29 JVM+iOS** · home 회귀 20/20. **주요 결정**: 계약 2회 개정(multipart·JWT 서빙 — *안 쓰는 유연성에 복잡도를 지불하지 않는다*) / 재제출 전면 거부 + §4 조회 회복 / 갤러리 이중 차단(plist 미선언 포함) / EXIF 앱 책임(서버측은 실사용자 유입 전 재검토 부채) / 조회 실패 시 CTA 노출 폴백(판정 번복 끝 확정) / 판정 feature 는 FCM 제외(8/25). **잡은 버그**: 같은 key 덮어쓰기 바꿔치기(409→multipart 로 소멸) / boundary 소실(전역 Content-Type) / `//` 조인 조용한 404 / 🔴 실기 2건 — 로거의 사진 스트림 소비(결정 3개의 교차점, JSON 테스트로는 영원히 못 잡는 구조), storage-root 이전 사진 404. **해소한 백로그**: 원격 이미지 로더 도입(friends 이월). **미해결**: 문구 4종 초안 / 디자이너 확인 8건 / iOS 촬영 실기기 / Coil 404·티커·CTA 잔여 검증 / 보관 정책·`photo_url` 개명 / 판정 feature.)
- **2026-08-07 — feature `push-fcm` 완료** (`completed`(Android). **앱이 사용자에게 먼저 말을 걸 수 있게 됐다** — `challenge-create`·`soul-oath` 등 세 feature에서 반복 지적되던 *"홈에 들어와야 도전장을 안다"* 결함 해소. `users.fcm_token`·`notifications`는 **V1부터 파여 있었고 배관만 없었다.** **백엔드**(`f01bd44` 42파일 +2999, `e76c64c` 문구): `PUT /users/me/fcm-token`(§0.2 **토큰 소유권 이전 2단계 UPDATE**), `logout` 실구현(foundation 미구현 TODO 흡수), `NotificationType` + V8(**DDL·DML 0건, `COMMENT ON COLUMN` 2건만**), `NotificationSender` 포트 + **`NoOpNotificationSender`** + `AFTER_COMMIT`/`REQUIRES_NEW` 디스패처. **254/254**(baseline 192 → 신규 62, 회귀 0). **모바일**(`d13dd9b` 36파일 +766, `27dfb96`, `799a71e`): **`:core:push` 신설**, `ChallengeFirebaseMessagingService`, moko-permission. 🔴 **실기 검증** — 알림 3종 전부 발화(`REQUEST` 5 / `REJECTED` 2 / `ACCEPTED` 1), 수신자 배분 정확(**거절만 신청자 쪽으로 방향 반전**), 그리고 **§0.2 소유권 이전이 실사용으로 증명**됐다(같은 기기 계정 전환 시 토큰이 15→16 이동, 15는 NULL). 그 SQL은 fake만 태우던 **무검증** 상태였다. **주요 결정**: 토큰 등록은 로그인 body가 아닌 독립 엔드포인트(FCM 토큰 수명주기가 로그인과 다르다) / **키 없어도 서버가 선다**(no-op 격리 — 덕분에 Firebase를 기다리지 않고 백엔드 전체 완성, 그리고 키 없이 돌린 실행이 오히려 격리를 실기 증명) / `reference_id`를 `challenge_id`로 못 박지 않음(*오늘의 사실이지 규약이 아니다*) / KMP 래퍼 미사용(카카오 SDK 선례 답습) / **`:core:push`는 소비자가 둘이 되는 순간 승격**(이 레포 기준은 "같은 SDK냐"가 아니라 "소비자가 누구냐"). **잡은 오류**: 계약이 서버가 안 보내는 `data` 키를 보낸다고 적음(`friends` ISO-8601과 같은 계열) / `jsonPath().doesNotExist()`가 `{"data":null}`에서도 통과 / `MainViewModel` 인증 게이트 누락(실기 401). ⛔ **iOS 수신 범위 제외 — 기술 선택이 아니라 APNs 키가 유료 Apple 계정 전용이라 경로가 존재하지 않는다**(시뮬레이터로도 우회 불가). ADR-0005가 처음 깨지는 지점. **미해결**: `LogoutUseCase` 서버 미호출 / `AFTER_COMMIT`·`REQUIRES_NEW` 실동작 무검증 / §0.2 SQL 자동 검증 부재 / 알림 목록 화면 / 딥링크 / 아이콘 placeholder.)
- **2026-08-03 — feature `soul-oath` (`partially-completed`)** (제품 컨셉의 **핵심 차별점** 구현 — *"영혼의 맹세(계약서)까지 작성해야 시작되는 진짜 약속"*. `challenge-create`가 의도적으로 건너뛰고 "나중에 삽입한다"고 적어둔 그 약속을 이행했다. **이제 양측이 서명해야만 챌린지가 시작된다.** 서명은 **벡터 스트로크를 DB에 저장** — 파일 스토리지 ADR을 이 feature 하나 때문에 세우지 않았다(카메라 인증 때 결정). **백엔드**: V7(`_signature_url` → `_signature_data` + 주석 6건), `Contract` 도메인, 신규 `GET /challenges/{id}`, 계약서 본문 **박제 + `is_finalized` 재렌더**. **171/171** + **실서버 56/56**(취소 FK / 바이트 왕복 3자 동일 / 완결 재렌더). **모바일**: `:feature:challenge:oath`·`:detail` 2모듈 신설, `:core:ui`에 `ContractCard`/`SignatureCanvas`/`SignatureView`, `AcceptChallengeDialog` 삭제 → 전체 화면. **230/230, 회귀 0 / 의도적 교체 5 / 신규 86.** **디자인**: design.md v7.1, Lovable `/oath` 프리뷰 동작, **신규 토큰 0건**. **주요 결정**: `CONTRACT_SIGNING` 미도입 + 수락·서명 **원자 요청**(도입하면 "수락 후 서명 전 이탈"이 어느 목록에도 안 잡혀 재개 불가 — `/sent` 부재와 동형. 부수 효과로 **교체 테스트 0건**) / 취소 시 contract **명시적 선삭제**(CASCADE 기각 — 컨셉상 조용한 연쇄 삭제 불가) / 상대 맹세 화면 **전체 화면**(근거는 면적이 아니라 **제스처 소유권** — 캔버스를 `verticalScroll`에 두면 획 긋기와 스크롤이 다툰다) / 종횡비 2:1을 **컴포넌트 내부에서 강제**(불변식은 파라미터로 열지 않는다). 🔴 **실기에서 버그 3건** — 서명 캔버스가 **획당 점 1개**(223건 초록인데 기능 무동작) / 맹세 화면 **100% 미개방**(PENDING의 `null` mission) / **전역** `UnknownApiError`가 `onError` 미호출로 **역직렬화 실패가 조용한 무한 로딩**(호출부 24곳). 🔴 **"테스트는 통과하는데 실제로는 틀린" 계열이 이 feature에서만 6번** — spec에 절로 정리. **검증이 판정에서 측정으로**: 종횡비 픽셀 실측 2.0000 / 입력↔렌더 잉크 bbox 차이 ≤ 0.0038 / 인셋은 대조군 계산 후 실측 / 최악값 20,150자를 **세 독립 구현**이 일치. **미해결**: 🔴 iOS 실기 미검증(시뮬레이터로 대체 안 함) / 주관 3건 / 자정 경계 / 위저드 백 배선 / 통합 45 skip / **3개 레포 커밋 0건**.)
- **2026-08-03 — feature `dev-test-login` (`partially-completed`)** (가짜 계정 3개 + debug 전용 로그인 버튼으로 **혼자 친구·챌린지 플로우를 검증할 수 있게** 했다 — `friends`·`challenge-create`의 "manual smoke" 미해결이 계속 쌓이던 병목. **인증 우회 경로라 격리를 기능과 동등한 수용 기준으로** 다뤘다. **백엔드**(`3b73627`): 커스텀 `Condition`(프로파일 미도입 — *프로파일은 의미가 누적돼 로깅·CORS 때문에 `dev`를 켜면 인증 우회까지 딸려 켜진다*), `AuthService.issueTokens` 추출로 카카오와 발급 경로 공유, permitAll **상시**+빈만 조건부(*fail-closed를 지키는 건 permitAll이 아니라 빈 조건부*), `NoResourceFoundException`→404 핸들러. **134/134** + **켠 상태 실서버 34/34**. 🔴 **`getProperty(Boolean::class)`가 오타에 서버를 죽인다는 발견** — `"enabled"`/`""`에 `ConversionFailedException`을 던진다. *fail-closed는 "안 열린다"여야지 "터진다"가 아니다* → 문자열 `"true"` 정확 비교. **모바일**(`d36b42e`): `TestLoginSection`(`isDebug` 게이트), `LoginWithTestAccountUseCase`가 `LogoutUseCase`를 내부 호출, **T-M4 `HomeTopBar` debug 계정 표시**(실측 결과 *앱에 "내가 누구인지" 보이는 화면이 0건*이었다 — 계정을 오가는 feature에서 치명적), `TokenLocalDataSource` interface 분리(backlog 조건 충족). **148/148, 회귀 0 / 삭제 0.** **실사용**: 테스트 계정으로 **친구 요청→수락 성공**(09:38:54→09:39:01), ADR-0010 KST 저장 신규 데이터로 재확인. **pm-lead 오류 3건을 팀이 잡았다** — "꺼진 상태=404" 수용 기준이 실제로는 401 / 계약 초안의 `nickname` 필드 부재 / "홈에 닉네임 보인다" 전제. **미해결**: 🔴 **격리 OFF 실서버 검증 미완**(핵심 수용 기준) / 챌린지 수락 경로 DB 증거 부재.)
- **2026-07-31 — feature `datetime-model-migration` 완료** (`completed`. ADR-0010 구현. 시간 표현을 **`Instant`+ISO-8601 UTC → `LocalDateTime`+KST+`yyyy-MM-dd HH:mm:ss`** 로 통일. 신규 엔드포인트 0건, 기존 `confirmed` 계약 3건의 표기만 개정한 횡단 변경. **🔴 착수 후 문제 정의가 뒤집혔다** — 초안은 "서버가 UTC 저장이니 KST로 바꾼다"였으나 실측 결과 **서버는 이미 KST였고**(`users` 1행에 `+9h` 쳤으면 **데이터 손상**), 진짜 문제는 **시계가 두 갈래로 갈라져 있던 것**(`ChallengeCommandService`만 `Clock.systemUTC()`=UTC, 나머지는 `LocalDateTime.now()`=KST → 같은 DB에 9시간 다른 두 기준). "DB TIMESTAMP를 UTC로 간주"라는 문서 규약을 레포 코드가 한 번도 안 지켰고, challenge-create가 그 규약을 충실히 따르며 **UTC 섬**을 만든 것이 드러났다. **백엔드**: `KstTime`(엔티티 기본값용) + `Clock.system(Asia/Seoul)` 병행, `WIRE_DATETIME` 상수 단일 출처 + `@JsonFormat` 명시(정본) + `JacksonDateTimeConfig`(안전망), V6(**DML 0건 + `COMMENT ON COLUMN` 17건** — offset을 잃는 리스크를 DB 메타데이터로 대응), `NoResourceFoundException`→404 핸들러. **123/123 passed**(이전 111) + **`TZ=UTC` 전 코드베이스 재실행 123/123**(안전망 없이 정본만으로 선다는 직접 증거) + **실서버 65/65 PASS**(`DB deadline == 응답 deadline` 직접 대조로 시각 이동 없음 증명, challenge-create 58건도 새 포맷에서 전부 통과). **모바일**: `kotlinx-datetime` 도입 — challenge-create에서 깨진 원인이 API 비호환이 아니라 **버전 스큐**(선언 0.6.2 / common 해석 0.7.1)였음이 규명돼 0.7.1 선언으로 해결. 손으로 짠 `floorDiv`+Hinnant `civil_from_days` 제거(테스트 9건 무수정 통과가 등가성 보장), `DISTANT_PAST` 센티넬 폐기 → 필드 중요도 분기(`deadline`=제외+`onError` / 나머지=nullable), **`:remote:mapper`에 `commonTest` 소스셋 신설**(mapper 레이어 전체가 미검증이었음). **123/123 passed** — 기존 87 회귀 0 / 의도적 삭제 1 / 신규 36(`WireFormatBaselineTest` 4건이 baseline 대조를 **사람 검수에서 자동 테스트로** 전환). Android·KMP·iOS 링크 SUCCESS. **CLAUDE.md 시간 규칙 개정 + confirmed 계약 3건에 대체 고지.** 미해결: 통합 45건 skip 유지 / 모바일↔서버 실연동(dev-test-login 후 손 검증) / iOS 유닛 / `api()` 전이 / **3개 레포 커밋 0건**.)
- **2026-07-31 — feature `challenge-create` 완료** (`completed`. 제품 핵심 행위 "친구에게 챌린지를 건다" 개통 — 생성 → `PENDING` → 수락/거절. 수락 즉시 `IN_PROGRESS`로 전이시켜(`CONTRACT_SIGNING` 건너뜀) 이번 feature 단독으로 "생성→수락→홈 노출" 확인 가능. **백엔드**: endpoint 5건 + V5(`opponent_mission` NULL 완화 + **부분 유니크 인덱스** `uq_challenges_active_pair_date` — READ COMMITTED에서 앱 레벨 검사만으로는 중복 금지 수용 기준을 만족 못 한다는 근거로 범위 확대 승인) + `KstDeadlineCalculator` + `ClockConfig`. **111/111 passed** + **실서버 end-to-end 58/58 PASS**(실 JWT HTTP 호출로 JPA 매핑·Flyway·Security 필터·직렬화 검증, DB 시드 전량 원복). **모바일**: `:feature:challenge:create` 2-step 위저드(스텁 6파일 → 실물 + 컴포넌트 12), `:feature:home` "받은 도전장" 섹션 + `AcceptChallengeDialog`, `:domain`/`:remote`/`:data` challenge 계층 신설. **88/88 passed**(`HomeViewModelTest` 21/21 = 기존 10 + 신규 11, 기존 10건 이름·단언 무변경) + Android·KMP·iOS 링크 + `xcodebuild` SUCCESS. **디자인**: design.md v3, Lovable 2파일, **신규 토큰 0건**. **주요 결정**: 보낸 도전장 목록 제외(옵션 C, 사용자) / 모바일이 에러 `code`를 소비 못 하므로 "실패 시 코드 무관 스낵바 + 항상 재조회" + 전 에러 문구 확정 / 수락 UI 바텀시트→다이얼로그 2회 번복(근거 1건은 계약 확정으로 무효화됐으나 결론 유지) / 컴포넌트는 feature 모듈 배치(`72d9d9c` 존중) / iOS는 ATS가 아니라 **LNP**가 관문(`NSLocalNetworkUsageDescription`만 추가, `NSAllowsLocalNetworking`은 no-op이라 미추가). **팀이 잡은 pm-lead 오류 3건**: spec T-M3가 사용자 커밋을 되돌림 / T-M4가 이미 완료된 작업 / (후속 feature에서) `users` 1행 `+9h` 보정이 데이터 손상. **미해결**: 통합 45건 미실행(런타임 부재) / 잘못된 요청 본문 500 / 시스템 백 미구현 / 실기기 검증 / **4개 레포 커밋 0건**.)
- **2026-07-02 — feature `friends` 2차 친구 추가 완료** (`completed`. 친구 시스템의 실제 동작 구현 — 검색·요청·수락·거절·취소·목록·받은요청 7 endpoint + 모바일 도메인/Data/ViewModel/UI + 카카오톡 초대. **백엔드**: `V1 friendships` 그대로 활용(마이그레이션 0건), `FriendController` + `FriendService` + 통합 4건 + 슬라이스 15/15 PASS + `saveAndFlush` race recovery + `escapeForLike` 단위테스트 6/6. 커밋 `bae8ab6` → `1d9d88d` push. **모바일 T3-T5**: 사용자가 `81eccf0`로 직접 커밋 (`FriendsViewModel` / `FriendsSearchViewModel` / ItemState / FakeFriendsRepository). **모바일 T6-T7b** (Agent Teams + 옵션 C): `:core:designsystem/friend/{FriendListItem, FriendRequestCard}` (@Preview 4건, 이니셜 placeholder) + `:feature:friends`에 화면·라우팅·컴포넌트 wire-up (`FriendsScreen` 확장 / `FriendsSearchScreen` 신규 / `FriendsActionRow, ReceivedRequestsSection, FriendsListSection` 3분할 / `FriendsSearchTopBar, FriendSearchItem` + 5-relation Preview) + `:core:invite` 모듈 신규 (interface KakaoInviter + Android/iOS expect-actual + KakaoInviteBridge iOS handler injection) + KakaoLink 초대 (SDK Default `TextTemplate`, 콘솔 templateId 발급 skip — spec §4.5 정정 2026-07-02, App Distribution 소규모 운용 결정 반영). **HEAD 드리프트 대응**: 사용자 커밋 `8a5e725`에서 `UserInfoRepository.observeUserInfoCache()` 제거 확인 → T7b `inviteFriend`는 `getUserInfo(CACHE_FIRST).firstOrNull()` + null 가드로 처리 (옵션 B). **테스트**: Android testDebugUnitTest 32/32 PASS (FriendsViewModelTest 10 [T7a 7 + invite 3] + FriendsSearchViewModelTest 12 + HomeViewModelTest 10, 회귀 0). Android/common 166 tasks BUILD SUCCESSFUL + iOS 컴파일 SUCCESS. iOS 유닛테스트 미실행(backlog). **Agent Teams 학습**: `Agent tool name parameter`로 spawn 검증 + 옵션 C(child claude 위임)로 컨벤션 강제 발화 + Bash 10분 캡 학습 후 `mobile-dev.md` 재갱신 "child = Edit only / 본체 = background 빌드" 새 표준 도입. PM hub 커밋 다수, 모바일 working tree 사용자 commit 대기.)
> 한도(10건) 초과로 2026-04-23 ADR-0001~0008 / 2026-04-24 foundation / 2026-04-24 auth-kakao(당시 partial) / 2026-04-30 `colors.md`+tokens.md 1차 동기 / 2026-04-30 designsystem Lovable 다크 1차 통합 항목은 [backlog-archive/2026-04.md](./backlog-archive/2026-04.md)로,
> 2026-05-28 auth-refresh-rotation / 2026-05-25 home-feed 항목은 [backlog-archive/2026-05.md](./backlog-archive/2026-05.md)로,
> 2026-06-15 home-feed v2 / 2026-06-24 friends 1차 항목은 [backlog-archive/2026-06.md](./backlog-archive/2026-06.md)로 이동.

---

## 갱신 규칙

1. **자동 추가**: pm-lead가 `report-and-document` 스킬로 feature 종료 시, 해당 summary.md의 "미해결 이슈"를 본 백로그의 적절한 우선순위 표에 추가. 출처 컬럼에 summary.md 링크.
2. **자동 이동**: 동일 스킬에서 백로그에 있던 항목이 이번 feature로 해결됐다면 "최근 완료" 섹션으로 이동.
3. **수동 정리**: 사용자가 "백로그 정리해줘" / "백로그 갱신" 요청 시 pm-lead가 모든 출처(features/*/summary.md, design ⚠️, ADR pending, repos.json blockers, 노션 PM Questions)를 다시 스캔하여 백로그를 재구성.
4. **항목 수명**: "최근 완료"는 10건까지만 본 파일에 두고, 초과 시 `docs/backlog-archive/{YYYY-MM}.md`로 이동(아카이브 파일이 처음 생성될 때만 디렉토리 생성).
5. **출처 무손실**: 백로그 항목은 항상 원본 출처 링크를 갖는다. 출처가 사라지면 백로그 항목도 검토하여 폐기/이동.
6. **우선순위 변경**: 기능 진행에 따라 🟡↔🟢 이동 자유. 외부 차단되면 🔵, 다음 sprint 시작을 차단하면 🔴.
