# 마이페이지 (mypage) — Spec

- **feature-id**: mypage
- **owner**: pm-lead
- **상태**: completed (2026-08-26 — [summary.md](./summary.md)) → 🔴 **후속 개정 진행 중 (2026-08-28)**: 보관함 조회 단위가
  **월 단위 이동**으로 뒤집혔다 (아래 §후속 개정 참조)

## 🔴 후속 개정 — 보관함 월 이동 (2026-08-28 사용자 확정)

**1차 구현의 "전체 최신순 + 월 섹션 헤더"를 폐기하고 월 단위 조회로 바꾼다.** 사용자 원래 문면
(*"월별로 조회해서 카드 형식"*)이 정본이었고, design 이 "월별로 정리"로 해석해 전체 스크롤로 간 것을
사용자가 확인 시점에 정정했다 (design.md ⑭ 확인 대상의 결론).

| 항목 | 확정 |
|---|---|
| 이동 단위 | **월 1칸씩 순차 이동** — 🔴 **빈 달도 건너뛰지 않고 그대로 지난다** ("이 달에는 기록이 없어요" 표시). 달력의 시간 흐름을 그대로 따르는 쪽 |
| 첫 진입 | **항상 이번 달**. 이번 달에 기록이 없으면 빈 달 화면이 첫 인상이 되는 것을 수용한다 (현재 시점 기준이 예측 가능하다는 판단) |
| 범위 경계 | 미래 달로는 가지 않는다 — 🔵 **UI 규칙이 아니라 데이터 사실**: `COMPLETED` 의 `challengeDate` 는 구조상 항상 오늘 이하라 미래 달에는 카드가 존재할 수 없다. ✅ **과거 하한 = 최초 `COMPLETED` 기록의 월** (2026-08-28 backend 확정): 무한 기각 — *"보관함은 '이게 네 기록 전부다'라고 주장하는 화면인데 하한이 없으면 그 주장을 할 수 없다"*. 가입월 기각 — 가입~첫 기록 사이가 **보장된 빈 달**이라 목적을 절반만 이룬다. **기록 0건이면 양쪽 화살표가 다 비활성**(아무 데도 없는데 뒤로 걷게 두면 "더 찾으면 있을지도"라는 거짓 기대) |
| 조회 방식 | 서버 월 파라미터. 1차의 전체 반환은 폐기 |

🔴 **1차 판단 근거 2개가 이 결정으로 무효가 된다 — 대체가 필요하다:**
1. *"빈 달을 연속으로 넘기게 된다"* → **사용자가 그 동작을 선택했다.** 대신 빈 달 화면이 자주 보이므로
   **빈 달 표시의 완성도가 중요해진다**(빈 상태 문구 + 이동이 막히지 않는다는 것이 명확해야 한다)
2. 🔴 *"홈 7일 노출이 지난 결과에도 무조작 도달"*(spec 수용 기준) → **이번 달에 기록이 없으면 깨진다.**
   백로그 🟡 "결과 히스토리 부재"의 해소 근거였던 항목이라, **월 이동으로 도달 가능하면 충족으로 볼지**
   판정이 필요하다 (pm-lead 판단: 도달 경로가 존재하고 조작이 1~2회면 충족으로 본다. 단 summary 의
   해당 수용 기준 문면을 정정한다)
- **생성**: 2026-08-26
- **사용자 확정 (2026-08-26)**: ① 탈퇴 정책 = **익명화 + 기록 보존 + 사진 삭제** ② 계약서 보관함 =
  **월별 조회 + 카드 목록** (캘린더 아님 — 캘린더는 통계 화면 feature 로 이월) ③ 보관함 상세 스펙은
  **design-bridge 협의로 확정**

## 배경

4탭 중 마지막 실속 없는 탭. 기획 IA 정의(내 프로필·통계·계약서 보관함·설정) 중 1차로
**프로필 카드 + 계약서 보관함 + 로그아웃 실구현 + 회원탈퇴 + 설정 골격**을 채운다.
계약서 보관함은 백로그 🟡 **"결과 히스토리 화면 부재"** (홈 7일 노출이 시간벌기)의 정식 해법이자
기획 §3.3(*"이때 너 이거 걸었잖아"* — 증거 보관)의 이행이다.

## §0 사전 실측

| | 상태 |
|---|---|
| 프로필 카드 데이터 | ✅ 전부 있음 — `GET /record`(승/패/무/연승, 실데이터) + UserInfo 캐시(닉네임·프사) |
| `:feature:mypage` | placeholder — Lovable `mypage.tsx` 디자인 존재 (⚠️ 캘린더 섹션은 이번 범위 제외) |
| 계약서 보관함 화면 | ❌ **Lovable 에 없음** (mypage 메뉴 링크만) — design-bridge 가 신규 명세 |
| 지난 챌린지 상세 | ✅ `GET /challenges/{id}` 가 기한 없이 열림 (계약서·서명·사진·결과) — 목록만 만들면 됨 |
| 로그아웃 | ⚠️ 백로그 🟡 — `LogoutUseCase` 가 서버 `DELETE /auth/logout` 을 안 부른다(엔드포인트는 구현·검증
  완료, 호출부 0). 🔴 선결 사실 2건이 백로그에 있다: `sendWithoutRequest` 술어가 `/auth/logout` 까지
  걸리는 문제(인증 플러그인 전역), `WWW-Authenticate` 실측. clearTokens **앞**에 서버 호출 |
| 회원탈퇴 | ❌ 전부 신규 — 기획서에도 없던 항목(스토어·카카오 요건). 카카오 unlink 방식은 backend 조사 |
| `PhotoStorage.delete()` | ✅ 구현돼 있으나 **호출부 0** (백로그) — 탈퇴의 사진 삭제가 첫 호출부가 된다 |

## 사용자 시나리오

1. MY 탭 → 내 프로필(닉네임·프사)과 전적(총 N챌린지·승률·승/패/무)을 본다.
2. 계약서 보관함 → **월 단위로 묶인 카드 목록**으로 지난 챌린지(상대·미션·내기·결과)를 훑고,
   카드를 탭해 기존 상세(계약서 원문·서명·인증 사진·결과)로 들어간다. *"이때 너 이거 걸었잖아."*
3. 설정 → 로그아웃: 서버 세션(refresh hash·FCM 토큰)까지 끊긴다 — 로그아웃한 기기로 푸시가 오지 않는다.
4. 설정 → 회원탈퇴: 경고 확인 후 탈퇴. 내 개인정보는 지워지되 **상대의 기록(계약서·결과)은 남는다** —
   상대 보관함에서 나는 "탈퇴한 사용자"로 보인다.

## 탈퇴 정책 (사용자 확정 — 익명화 + 기록 보존 + 사진 삭제)

| 대상 | 처리 | 근거 |
|---|---|---|
| `users` 개인정보 (kakao_id·nickname·profile_image_url·phone hash·fcm_token·refresh hash) | **초기화/익명화** — 닉네임 "탈퇴한 사용자" | 개인정보 삭제 의무 |
| 카카오 연결 | **unlink** (방식은 backend 조사 — admin key vs 사용자 토큰) | 카카오 요건 |
| `friendships` | **삭제** — 랭킹은 친구 기반이라 자연 제외 | 관계 종료 |
| 챌린지·계약서·서명·결과·전적(`user_stats`·상대의 `friend_records`) | 🔴 **보존** | 양자 데이터 — 상대의 증거. 컨셉("증거 보존")의 요구 |
| **인증 사진 파일** | 🔴 **삭제** (`PhotoStorage.delete` 첫 호출부) — 상대 화면에는 "탈퇴한 사용자의 사진" 류 표시 | 얼굴이 남는 문제와 증거 보존의 절충 |
| 진행 중(`IN_PROGRESS`) 챌린지 | 오픈 이슈 §리스크 — **자연 판정 권장**(탈퇴자는 인증 불가 → 배치가 상대 승/양패 처리) | 판정 규칙 사본을 안 만든다 |
| 탈퇴자가 보낸 `PENDING` 신청 | **삭제** (2026-08-26 pm-lead 확정 — backend 공백 지적) | 보존할 증거가 0(계약서·서명·사진 전무)이고, 남기면 상대가 수락해 **결과가 정해진 챌린지**를 새로 시작할 수 있다. 기존 "챌린저의 PENDING 취소"(`deleteById`)와 의미 동일. 탈퇴자가 받은 `PENDING` 은 수락 불가라 자연 만료(EXPIRED 배치) |
| 탈퇴자의 `notifications` (수신함) | **삭제** (2026-08-26 pm-lead 확정) | 본인 전용 개인 데이터 — 상대가 읽는 값이 아니라 "양자 기록 보존" 원칙과 무관하고, 개인정보 삭제 축에 속한다. 로그인 불가로 아무도 못 읽는 죽은 데이터를 남길 이유가 없다 |

## 수용 기준

- [ ] 프로필 카드: 닉네임·프사(이니셜 placeholder)·총 챌린지·승률·승/패/무 표시 (기존 API 재사용, 신규 0)
- [ ] 보관함: **월별 그룹 카드 목록** — 종료된(COMPLETED·EXPIRED 제외 여부는 협의) 챌린지, 카드 탭 → 기존 상세
- [ ] 보관함으로 **홈 7일 노출이 지난 결과에도 도달**할 수 있다 (백로그 히스토리 항목 해소 조건)
- [ ] 로그아웃: 서버 호출로 `refresh_token_hash`·`fcm_token` 이 무효화되고, **그 기기로 푸시가 더 안 온다**
- [ ] 로그아웃/탈퇴 후 로그인 화면으로 이동, 로컬 캐시(토큰·UserInfo) 삭제
- [ ] 탈퇴: 위 정책표대로 — 탈퇴 후 상대가 지난 챌린지 상세를 열면 **깨지지 않고** "탈퇴한 사용자"로 보인다
- [ ] 탈퇴한 사용자의 인증 사진 요청은 깨지지 않는 실패(404 계열)로 처리되고 화면이 대체 표시를 낸다
- [ ] 탈퇴는 **경고 확인 단계**를 거친다 (되돌릴 수 없음 고지)
- [ ] 테스트 결과 숫자로 (Android+iOS)

## 비범위

- **월별 달성 캘린더 / 통계 화면** — 별도 feature (사용자 확정: 보관함은 카드 목록으로 간다)
- 알림 설정(on/off) — 끌 대상이 아직 4종뿐, 설정은 골격만
- 친구 프로필 / 1:1 전적 화면 — 별도 feature
- 프로필 편집(닉네임 변경 등) — 카카오 연동값 사용 유지
- 랭킹 바로가기 메뉴 — 탭과 중복이라 제외 제안 (design 협의)

## 태스크 분해

### 디자인 (design-bridge) — 🔴 이번엔 스펙 확정의 공동 주체다

- [ ] **T-D1. mypage 화면 명세** — Lovable `mypage.tsx` 기반. ⚠️ 캘린더 섹션 제외 판정, 메뉴 항목
  확정(통계는 "준비 중"인지 미노출인지), 로그아웃·탈퇴 배치
- [ ] **T-D2. 계약서 보관함 화면 제안** — 🔴 **Lovable 에 없는 신규 화면.** 사용자 지시가 출발점:
  **"월별로 조회해서 카드 형식으로 정리"**. 월 헤더 + 카드(상대·미션·내기·결과 뱃지) 형태 제안,
  기존 카드 문법(홈 ChallengeCard·ChallengeOutcomePill) 재사용 검토, 빈 상태·탈퇴 상대 표시.
  **mobile·backend 와 협의해 조회 단위(월 파라미터 vs 전체+클라 그룹핑)에 디자인 근거를 보태라**

### 백엔드 (backend-dev)

- [ ] **T-B1. 보관함 목록 API** — 월별 조회 shape 을 계약으로 확정 (파라미터·정렬·EXPIRED 포함 여부·
  페이지 정책). `confirmed` 계약 변경 없음 예상 (신규 1건)
- [ ] **T-B2. 회원탈퇴 API** — 정책표 이행. 카카오 unlink 방식 조사 포함. 🔴 익명화 후에도 기존
  응답들(상세·보관함·랭킹 아님)이 깨지지 않는지 — nickname 등 non-null 계약 유지
- [ ] **T-B3. 로그아웃** — 서버는 구현돼 있음. 모바일 연동 과정에서 계약 명세 확인만
- [ ] 사진 삭제 = `PhotoStorage.delete` 첫 호출부 — 백로그 항목 갱신 대상

### 모바일 (mobile-dev)

- [ ] **T-M1. 프로필 카드 + 마이페이지 골격** (기존 API 재사용 — 계약 무관, 선착수 가능)
- [ ] **T-M2. 로그아웃 실구현** — 🔴 백로그의 선결 2건(`sendWithoutRequest` 술어 축소 + `WWW-Authenticate`
  실측)과 필수 사실 2건(clearTokens 앞 호출·소유권 이전과 비중복) 준수. 인증 플러그인 전역 변경이라
  회귀 주의
- [ ] **T-M3. 계약서 보관함 화면** — 계약+design 확정 후
- [ ] **T-M4. 회원탈퇴 플로우** — 경고 다이얼로그 → 탈퇴 → 로그인 이동
- [ ] **T-M5. 테스트** — 숫자로

## 의존 관계

```
T-D1·T-D2(design) ─┬─→ 계약 협의(T-B1 shape) → confirmed → T-M3
T-B2·T-B3 ─────────┴─→ confirmed → T-M2·T-M4
T-M1 은 즉시 착수 가능 (계약 무관)
```

## 리스크 / 오픈 이슈

- **진행 중 챌린지가 있는 사용자의 탈퇴** — 권장: 막지 않고 자연 판정(탈퇴자는 인증 불가 → 자정 배치가
  기존 규칙대로 처리). 대안: 탈퇴 시점 즉시 포기 처리(판정 규칙 사본이 생겨 비권장). 계약 협의로 확정
- **탈퇴 직후 상대 화면의 정합** — 상세·보관함·랭킹(친구 삭제로 제외)·홈 카드 각각에서 "탈퇴한 사용자"
  표시가 깨지지 않아야 한다. nickname 을 익명값으로 유지하면 계약 변경 0 으로 갈 수 있는지가 관건
- **카카오 unlink 실패 시** — unlink 와 DB 익명화의 순서·부분 실패 처리 (backend 설계)
- 로그아웃의 인증 플러그인 전역 변경 — 기존 401 자동 갱신 회귀 표면
