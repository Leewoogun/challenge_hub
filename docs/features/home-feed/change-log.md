| 일시 | 변경 | 사유 / 영향 |
|---|---|---|
| 2026-06-15 | **API 1개 → 2개로 분리** (v2). `GET /api/v1/home` 폐기 → `GET /api/v1/record` + `GET /api/v1/challenges/active` 신설. 전적 도메인 `UserStats` → `UserRecord` 리네임(DB 테이블 `user_stats`는 `@Table(name=...)` 매핑으로 유지). 모바일 Repository를 `Flow<T> + onError 콜백 + AuthEventBus` 표준 패턴으로 재정렬 — 기존 `sealed HomeResult { Success | Unauthorized | Error }` 폐기. UiState는 `Loading \| Data` 두 분기로 단순화(슬롯 분리 제거). `HomeViewModel`은 `GetHomeDataUseCase`로 두 Flow 결합. 401은 repository 내부에서 `AuthEventBus.emitSessionExpired()`로 전역 처리(`HomeUiEffect.NavigateToLogin` 제거). | 전적/챌린지 캐시 주기를 따로 가져갈 수 있게 됨. 모바일 신규 Repository 패턴이 `LoginRepositoryImpl`과 동일해져 일관성 확보. 서버 신규 커밋 `a80caa6 feat: 홈 화면 데이터 뿌려주는 api (챌린지 현황, 전적) 구현`. 모바일 working tree 변경 — 사용자 커밋 대기. 통합 테스트도 `HomeIntegrationTest` → `RecordApiIntegrationTest`(3 케이스) + `ActiveChallengeApiIntegrationTest`(3 케이스)로 분리, Docker 미가용 환경 skip 유지. `HomeViewModelTest` 7/7 passed(Android JVM). |
| 2026-05-25 | `confirmed` — 모바일/백엔드 양측 구현 완료 후 contract 변경 0건으로 종료. | api-contract.md draft → confirmed 전환. |
