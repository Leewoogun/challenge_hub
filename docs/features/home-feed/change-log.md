| 일시 | 변경 | 사유 / 영향 |
|---|---|---|
| 2026-08-25 | 🔴 **`GET /api/v1/challenges/active` 확장 (challenge-result)** — ① 상태 필터에 **`COMPLETED` 한시 노출** 추가(`completed_at > now - 7일`. `EXPIRED` 는 계속 제외). ② `ActiveChallengeDto` 에 **필드 3개 추가**: `status`(non-null, `IN_PROGRESS`\|`COMPLETED` 만) / `result`(nullable, **역할 기준**) / `myResult`(nullable, **내 시점** `WIN`\|`LOSE`\|`DRAW`). ③ **정렬 규칙 변경** — `deadline` ASC 단일 → **`IN_PROGRESS`(deadline ASC) 먼저, `COMPLETED`(completed_at DESC) 나중**. ④ 백엔드 쿼리 **3 → 4개**. ⑤ `GET /api/v1/record` 는 **무변경**. | **필드 추가·확장만이고 기존 필드의 이름·타입·의미 변경은 0건**이다 — 모바일이 새 필드를 무시하면 기존 동작이 그대로다(`ignoreUnknownKeys = true`, 전 필드 기본값 보유 — 실측). 🔴 **`COMPLETED` 를 섞는 이유는 UI 취향이 아니라 도달 가능성이다**: 판정 배치가 붙으면 마감된 챌린지가 `COMPLETED` 로 전이되는데, 1차 정책대로면 **판정되는 순간 홈에서 사라지고** 지난 챌린지 화면도 없고 결과 푸시도 없어(FCM 범위 제외 — 사용자 결정) **결과를 볼 방법이 0 이 된다.** `ActiveChallengeService.ACTIVE_STATUSES` 가 정확히 이 확장을 예고하고 만든 자리였다. ⚠️ **7일은 정식 해법이 아니라 시간벌기** — 창이 닫히면 그 챌린지는 앱 어디에서도 도달 불가다(서버 row 는 남고 `GET /challenges/{id}` 는 기한 없이 열린다. 못 보는 건 **목록**뿐). 지난 챌린지 목록 화면 + 조회 API 가 정식 해법이며 [백로그](../../backlog.md) 등재. 값은 서버 프로퍼티(`challenge.result.completed-retention-days`). ⚠️ `myVerificationStatus` 가 `COMPLETED` 카드에서 **`PENDING` 이 아니라 `FAILED`** 로 온다(배치가 미인증 측을 전이) — `:core:ui` `VerificationStatusPill` 이 `FAILED` 를 이미 그린다(실측). ⚠️ 빈 목록의 의미가 "진행 중 없음" → "진행 중도 없고 최근 7일 결과도 없음" 으로 넓어진다. **`result` 와 `myResult` 를 둘 다 준 이유**: `result` 는 역할 기준인데 이 응답에는 *"내가 challenger 인가"* 를 알 정보가 없어(닉네임만 있고 역할이 없다) 앱이 뒤집을 수 없다. 상세(`GET /challenges/{id}`)는 계약서 화면이라 `result` 만 주고 역할 기준을 유지한다 — **같은 데이터라도 화면 성격이 다르면 시점이 다르다.** |
| 2026-08-25 | **`user_stats` 집계 규칙 — 문장 변경 0건, 드디어 실행됨** (challenge-result). `GET /api/v1/record` 응답 shape·필드·타입 **전부 무변경**. | 🔴 **이 계약은 2026-05-25 부터 집계 규칙 5줄(`CHALLENGER_WIN`→win+1 … `currentStreak` = win 만 연속, lose/draw/both_lose 시 0)을 갖고 있었는데 `user_stats` 에 쓰는 코드가 0건이었다** — 테이블이 늘 비어 있어 이 엔드포인트가 항상 0 을 반환했다. 판정 배치가 집계를 붙이면서 **규칙을 한 글자도 바꾸지 않고 이행**한다. 부수 효과로 **홈 StatsBar 가 이 시점부터 실데이터**가 된다. ⚠️ 이 때문에 challenge-result spec 이 *"DRAW/BOTH_LOSE 의 streak 의미"* 를 오픈 이슈로 올렸으나 **협의할 게 없었다 — 이 `confirmed` 계약과 모바일 `UserRecord` KDoc 에 이미 확정돼 있었다.** 교훈: **집계(생산) 규칙이 그 값을 *읽는* 엔드포인트 계약에 살고 있었고**, 판정 스펙 작성 시 `GET /record` 계약을 보지 않아 없는 쟁점이 만들어졌다. ⚠️ `EXPIRED` 는 집계 제외(계약서 미체결 = 승부 미성립). ⚠️ 개돼지 랭킹의 **연패**는 `currentStreak` 으로 표현 불가(부호를 섞으면 `maxStreak` 의미가 무너지고 `"N🔥"` 표기가 깨진다). 🔴 **별도 컬럼 `current_loss_streak`/`max_loss_streak` 을 V10 으로 같은 feature 에서 추가했다**(pm-lead 승인) — 규칙은 **연승의 거울상**(패·양패 +1 / 승·**무승부** 0). **`GET /record` 응답은 무변경**이고 읽는 화면도 아직 없다. 지금 넣은 이유는 나중이면 `completed_at` 순회 **백필**이 필요해져서다(연패는 순서 의존이라 합계로 산출 불가) — 비용의 크기가 아니라 종류가 바뀐다. |
| 2026-06-15 | **API 1개 → 2개로 분리** (v2). `GET /api/v1/home` 폐기 → `GET /api/v1/record` + `GET /api/v1/challenges/active` 신설. 전적 도메인 `UserStats` → `UserRecord` 리네임(DB 테이블 `user_stats`는 `@Table(name=...)` 매핑으로 유지). 모바일 Repository를 `Flow<T> + onError 콜백 + AuthEventBus` 표준 패턴으로 재정렬 — 기존 `sealed HomeResult { Success | Unauthorized | Error }` 폐기. UiState는 `Loading \| Data` 두 분기로 단순화(슬롯 분리 제거). `HomeViewModel`은 `GetHomeDataUseCase`로 두 Flow 결합. 401은 repository 내부에서 `AuthEventBus.emitSessionExpired()`로 전역 처리(`HomeUiEffect.NavigateToLogin` 제거). | 전적/챌린지 캐시 주기를 따로 가져갈 수 있게 됨. 모바일 신규 Repository 패턴이 `LoginRepositoryImpl`과 동일해져 일관성 확보. 서버 신규 커밋 `a80caa6 feat: 홈 화면 데이터 뿌려주는 api (챌린지 현황, 전적) 구현`. 모바일 working tree 변경 — 사용자 커밋 대기. 통합 테스트도 `HomeIntegrationTest` → `RecordApiIntegrationTest`(3 케이스) + `ActiveChallengeApiIntegrationTest`(3 케이스)로 분리, Docker 미가용 환경 skip 유지. `HomeViewModelTest` 7/7 passed(Android JVM). |
| 2026-05-25 | `confirmed` — 모바일/백엔드 양측 구현 완료 후 contract 변경 0건으로 종료. | api-contract.md draft → confirmed 전환. |
